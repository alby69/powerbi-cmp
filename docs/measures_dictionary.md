# Measures Dictionary

This document lists all DAX measures defined in the model, organized by table.

## CALC_PreviousPeriod

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Value Previous |  | `SUMX('CALC_PreviousPeriod','CALC_PreviousPeriod'[PreviousPeriod])` |

## FACT_Mix

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| # Qta BIL MIX |  | `SUM('FACT_Mix'[# Qta BIL])` |
| Qta BIL MIX |  | `SUMX('FACT_Mix','FACT_Mix'[Qta BIL])` |
| Costo Mix |  | `CALCULATE(<br>DIVIDE([Costo Totale],[Qta BIL],0),<br>FILTER(DIM_Clienti,DIM_Clienti[CodiceCliente] IN {"C01049","C00991"}<br>)<br>)` |
| Importo Costo Mix |  | `CALCULATE(<br>[Costo Totale],<br>FILTER(DIM_Clienti,DIM_Clienti[CodiceCliente] IN {"C01049","C00991"}<br>))` |
| Costo Mix Resa |  | `CALCULATE(<br>DIVIDE([Costo Totale],[Qta Resa],0),<br>FILTER(DIM_Clienti,DIM_Clienti[CodiceCliente] IN {"C01049","C00991"}))` |
| Qta RESA MIX |  | `SUMX('FACT_Mix','FACT_Mix'[Qta Resa])` |

## FACT_Propano

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Costo Propano |  | `DIVIDE([Total Costs NAVI],[Qta Magazzino Acquisto IPEM CD])` |
| Importo Costo Propano |  | `[Total Costs NAVI]` |

## FACT_Vendite

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Qta Tab Vendite | Somma quantità tabella vendite | `// Somma totale delle quantità presenti nella tabella FACT_Vendite<br>SUMX('FACT_Vendite', 'FACT_Vendite'[Quantity])` |
| # Qta Vendite | Conteggio record vendite | `// Conteggio totale dei movimenti di vendita (basato sulla colonna CountQta)<br>SUM('FACT_Vendite'[CountQta])` |

## KPI_AcquistiDocumento

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Importo Acquisti (Doc) | Importo totale acquisti basato su documenti | `// Somma totale degli importi dai documenti di acquisto<br>SUMX ( 'FACT_DocumentoAcquisto', 'FACT_DocumentoAcquisto'[Importo] )` |
| Importo Materia Prima (Doc) | Importo acquisti materia prima (Articolo 4020002) | `// Importo acquisti filtrato per il codice articolo materia prima '4020002'<br>CALCULATE (<br>[Importo Acquisti (Doc)],<br>'FACT_DocumentoAcquisto'[CodiceArticolo] = "4020002"<br>)` |
| Qta Acquisti (Doc) | Quantità totale acquisti basata su documenti | `// Somma totale delle quantità dai documenti di acquisto<br>SUMX ( 'FACT_DocumentoAcquisto', 'FACT_DocumentoAcquisto'[Quantity] )` |

## KPI_CMP

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Qta CMP | Quantità BIL per il calcolo del CMP | `// Quantità bilanciata per il calcolo del Costo Medio Ponderato<br>SUM(FACT_MagazzinoCMP[Qta_BIL])` |
| Costo Medio Acquisto | Costo medio di acquisto per il calcolo del CMP | `// Rapporto tra importo acquisto CMP e quantità CMP<br>DIVIDE([Imp_Acquisto_CMP], [Qta CMP])` |
| Giacenza Quantità (CMP) | Quantità in giacenza per il calcolo del CMP | `// Restituisce la quantità progressiva di stock all'ultima data utile del periodo<br>CALCULATE(<br>MAX(FACT_Stock[QtaProgressiva]),<br>LASTDATE(DIM_Calendario[Date])<br>)` |
| Giacenza Valore (CMP) | Valore in giacenza per il calcolo del CMP | `// Restituisce il valore progressivo di stock all'ultima data utile del periodo<br>CALCULATE(<br>MAX(FACT_Stock[ValoreProgressivo]),<br>LASTDATE(DIM_Calendario[Date])<br>)` |
| CMP (Calcolato) | Costo Medio Ponderato calcolato su stock progressivo | `// Calcola il CMP come rapporto tra Valore Stock e Quantità Stock<br>VAR Valore = [Giacenza Valore (CMP)]<br>VAR Quantita = [Giacenza Quantità (CMP)]<br>RETURN<br>DIVIDE(Valore, Quantita)` |
| Costo Venduto (CMP) | Costo del venduto basato su CMP calcolato | `// Valorizzazione del venduto utilizzando il CMP calcolato<br>SUM(FACT_Stock[Quantità]) * [CMP (Calcolato)]` |
| Giacenza Quantità Periodo | Somma quantità stock nel periodo | `// Somma delle quantità progressive nel periodo selezionato<br>SUM(FACT_Stock[QtaProgressiva])` |
| Giacenza Valore Periodo | Somma valore stock nel periodo | `// Somma dei valori progressivi nel periodo selezionato<br>SUM(FACT_Stock[ValoreProgressivo])` |
| Importo CMP | Importo totale movimenti CMP | `// Somma totale degli importi in FACT_MagazzinoCMP<br>SUM(FACT_MagazzinoCMP[Importo])` |

## KPI_Magazzino

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Qta Magazzino | Quantità in magazzino in tonnellate (da Kg) | `// Calcola la quantità totale in magazzino convertendo Kg in Tonnellate<br>VAR QtaKg = SUMX ( FACT_Magazzino, FACT_Magazzino[Quantity] )<br>RETURN<br>DIVIDE ( QtaKg, 1000, 0 )` |
| Qta Magazzino Progressivo | Saldo progressivo della quantità in magazzino | `// Quantità in magazzino accumulata nel tempo (saldo ad oggi)<br>CALCULATE (<br>[Qta Magazzino],<br>FILTER (<br>ALL ( DIM_Calendario ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| Qta Acquisti | Quantità acquistata totale (Resa Mix + Quantity IPEM Propano) | `// Somma le quantità acquistate in base al gruppo prodotto (MIX o PROPANO)<br>VAR Gruppo = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () )<br><br>VAR QtaMix = [Qta RESA MIX]<br>VAR QtaPropano = SUMX(FACT_Propano, FACT_Propano[QuantityIPEM] )<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>Gruppo = "MIX", QtaMix,<br>Gruppo = "PROPANO", QtaPropano,<br>QtaMix + QtaPropano // Default: somma entrambi<br>)` |
| Importo Magazzino | Importo totale movimenti di magazzino da CG | `// Importo totale dei movimenti di magazzino registrati in CG<br>CALCULATE(<br>SUMX(FACT_MovimentiCG, FACT_MovimentiCG[Importo]),<br>REMOVEFILTERS(FACT_MovimentiCG[CodiceCategoria])<br>)` |
| Importo Fornitori | Debiti verso fornitori (Conto CG 2030001) | `// Importo dei movimenti CG filtrati per il conto fornitori '2030001'<br>CALCULATE (<br>[Importo Magazzino],<br>FACT_MovimentiCG[CodiceConto] = "2030001"<br>)` |
| Importo Acquisti (CG) | Ricavi/Costi acquisti da CG (Conto 4020002) | `// Importo degli acquisti registrati sul conto '4020002'<br>CALCULATE (<br>ABS ( [Importo Magazzino] ),<br>FACT_MovimentiCG[CodiceConto] = "4020002"<br>)` |
| Importo per Data Documento | Importo movimenti CG basato sulla data documento | `// Calcola l'importo CG utilizzando la relazione con la data del documento anziché la registrazione<br>CALCULATE (<br>SUMX ( FACT_MovimentiCG, FACT_MovimentiCG[Importo] ),<br>USERELATIONSHIP ( DIM_Calendario[Date], FACT_MovimentiCG[DataDocumento] )<br>)` |
| Importo Acquisti Altro | Importo altri acquisti (Conto 3100001, Progetto PS01) | `// Importo per acquisti vari su conto '3100001' e progetto 'PS01'<br>CALCULATE (<br>ABS ( [Importo Magazzino] ),<br>FACT_MovimentiCG[CodiceConto] = "3100001" &&<br>FACT_MovimentiCG[Progetto] = "PS01"<br>)` |
| Importo Accantonamenti | Totale accantonamenti | `// Somma totale degli accantonamenti registrati<br>SUMX ( FACT_Accantonamenti, FACT_Accantonamenti[Importo] )` |
| Importo Qta Ton Progressivo YTD | Valorizzazione della giacenza progressiva annuale al costo di acquisto | `// Valorizzazione della giacenza YTD al costo di acquisto unitario<br>[Qta Magazzino Progressivo YTD] * [Costo Acquisto Qta Ton]` |
| Importo Qta Ton | Valorizzazione quantità magazzino al costo acquisto | `// Valorizzazione della giacenza istantanea al costo di acquisto<br>[Qta Magazzino] * [Costo Acquisto Qta Ton]` |
| Importo Qta Ton Differenza CY-LY | Differenza di valorizzazione giacenza YTD tra anno corrente e precedente | `// Delta di valorizzazione tra l'anno corrente e l'anno precedente (stesso periodo)<br>VAR ValCY = [Importo Qta Ton Progressivo YTD]<br>VAR ValLY = CALCULATE([Importo Qta Ton Progressivo YTD], SAMEPERIODLASTYEAR(DIM_Calendario[Date]))<br>RETURN<br>ValCY - ValLY` |
| Importo Qta Ton Vendita | Valorizzazione quantità venduta al costo acquisto | `// Costo del venduto calcolato su quantità venduta e costo acquisto unitario<br>[Qta Vendite] * [Costo Acquisto Qta Ton]` |
| Importo Magazzino Progressivo | Importo progressivo movimenti magazzino per deposito IPEM | `// Importo progressivo dei movimenti di magazzino filtrato per deposito IPEM (dal 2021)<br>VAR Articolo = SELECTEDVALUE ( DIM_Articoli[CodiceArticolo] )<br>VAR AnnoInizio = 2021<br>VAR TabellaFiltrata =<br>FILTER (<br>ALL ( FACT_Magazzino ),<br>FACT_Magazzino[CodiceDeposito] = "IPEM" &&<br>FACT_Magazzino[CodiceArticolo] = Articolo &&<br>FACT_Magazzino[Date] >= DATE ( AnnoInizio, 1, 1 ) &&<br>FACT_Magazzino[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>RETURN<br>CALCULATE ( [Importo Magazzino], TabellaFiltrata )` |
| Importo Magazzino Progressivo (2) | Valorizzazione progressiva acquisti (CMP x Qta) | `// Valorizzazione progressiva: CMP Acquisto x Quantità Acquisto<br>[CMP Acquisto Mag Progr] * [Qta Acquisto Progr]` |
| Importo Qta Ton Previsto | Importo magazzino previsto basato su giacenza iniziale 2021 | `// Calcola l'importo previsto di magazzino partendo da una giacenza iniziale al 01/01/2021<br>VAR Gruppo = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto] )<br>VAR DataCorrente = SELECTEDVALUE ( DIM_Calendario[Date] )<br><br>VAR ImportoIniziale =<br>SUMX (<br>FILTER (<br>FACT_Giacenza,<br>FACT_Giacenza[Date] = DATE ( 2021, 1, 1 ) &&<br>FACT_Giacenza[GruppoProdotto] = Gruppo<br>),<br>FACT_Giacenza[Importo]<br>)<br>VAR TabellaFiltroCalendario =<br>FILTER (<br>ALLSELECTED ( DIM_Calendario ),<br>DIM_Calendario[Date] < MAX ( DIM_Calendario[Date] )<br>)<br>RETURN<br>CALCULATE (<br>IF (<br>DataCorrente = DATE ( 2021, 1, 1 ) \|\| ISBLANK ( [Qta Magazzino] ),<br>ImportoIniziale,<br>[Qta Magazzino]<br>),<br>TabellaFiltroCalendario<br>)` |
| Importo Vendite (CG) | Ricavi vendite da CG (Conti 30*) | `// Ricavi totali dalle vendite registrate sui conti CG 30*<br>CALCULATE (<br>ABS ( [Importo Magazzino] ),<br>FACT_MovimentiCG[CodiceConto] IN { "3000002", "3000003", "3000004", "3000005" }<br>)` |
| Importo Passaggi (CG) | Ricavi passaggi da CG (Conti 31*) | `// Ricavi totali dai passaggi registrati sui conti CG 31*<br>CALCULATE (<br>ABS ( [Importo Magazzino] ),<br>FACT_MovimentiCG[CodiceConto] IN { "3100001", "3100002", "3100003", "3100004", "3100005" }<br>)` |
| Importo Rimanenze Iniziali Propano (CG) | Importo rimanenze iniziali Propano (Conto 4010002) | `// Valore delle giacenze iniziali di Propano (Conto 4010002)<br>CALCULATE (<br>ABS ( [Importo Dare (CG)] ),<br>FACT_MovimentiCG[CodiceConto] = "4010002"<br>)` |
| Importo Rimanenze Finali Propano (CG) | Importo rimanenze finali Propano (Conto 3300002) | `// Valore delle giacenze finali di Propano (Conto 3300002)<br>CALCULATE (<br>ABS ( [Importo Dare (CG)] ),<br>FACT_MovimentiCG[CodiceConto] = "3300002"<br>)` |
| Importo CMP | Importo totale per calcolo CMP (Iniziali + Acquisti) | `// Base di calcolo per il Costo Medio Ponderato: Giacenza Iniziale + Acquisti<br>[Importo Rimanenze Iniziali Propano (CG)] + [Importo Acquisti (CG)]` |
| Importo Inventario | Valorizzazione inventario (Acquisti + CD IPEM) x CMP | `// Valore totale dell'inventario: (Qta Acquisti + Qta Giacenza IPEM) x CMP<br>([Qta Acquisti] + [Qta Magazzino CD Giacenza IPEM]) * [CMP Acquisto]` |
| Importo Rimanenze Finali Mix (CG) | Importo rimanenze finali Mix (Conto 3300003) | `// Valore delle giacenze finali di Mix (Conto 3300003)<br>CALCULATE (<br>ABS ( [Importo Dare (CG)] ),<br>FACT_MovimentiCG[CodiceConto] = "3300003"<br>)` |
| Importo Rimanenze Iniziali Mix (CG) | Importo rimanenze iniziali Mix (Conto 4010003) | `// Valore delle giacenze iniziali di Mix (Conto 4010003)<br>CALCULATE (<br>ABS ( [Importo Dare (CG)] ),<br>FACT_MovimentiCG[CodiceConto] = "4010003"<br>)` |
| Qta Magazzino Acquisto IPEM CD | Quantità acquisti in Conto Deposito deposito IPEM | `// Quantità acquistata per il deposito IPEM in regime di Conto Deposito<br>CALCULATE (<br>[Qta Magazzino CD],<br>FACT_ContoDeposito[TipoMovimento] = "Acquisto" &&<br>FACT_ContoDeposito[CodiceDeposito] = "IPEM"<br>)` |
| Qta Magazzino CD | Quantità totale movimenti in Conto Deposito | `// Somma delle quantità presenti in Conto Deposito<br>SUMX ( FACT_ContoDeposito, FACT_ContoDeposito[Quantity] )` |
| Qta Magazzino Acquisto IPEM CD Progressivo | Somma progressiva acquisti CD per deposito IPEM | `// Saldo progressivo degli acquisti in Conto Deposito per IPEM<br>CALCULATE (<br>[Qta Magazzino Acquisto IPEM CD],<br>FILTER (<br>ALL ( DIM_Calendario ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| Qta Magazzino CD Progressivo | Giacenza progressiva in Conto Deposito con gestione dei set-point 'Giacenza' | `// Calcola la giacenza progressiva in Conto Deposito gestendo correttamente i movimenti di tipo 'Giacenza' (set point)<br>VAR InizioPeriodo =<br>CALCULATE(<br>MIN(FACT_ContoDeposito[DataMovimento]),<br>ALLSELECTED(FACT_ContoDeposito)<br>)<br>VAR DataCorrente = MAX(FACT_ContoDeposito[DataMovimento])<br>VAR OrdineCorrente = MAX(FACT_ContoDeposito[EntryNo])<br><br>VAR UltimaGiacenzaPrecedente =<br>MAXX(<br>FILTER(<br>ALL(FACT_ContoDeposito),<br>FACT_ContoDeposito[TipoMovimento] = "Giacenza" &&<br>FACT_ContoDeposito[DataMovimento] <= InizioPeriodo<br>),<br>FACT_ContoDeposito[DataMovimento]<br>)<br><br>VAR Tab_Filtro =<br>FILTER(<br>ALLSELECTED(FACT_ContoDeposito),<br>(<br>FACT_ContoDeposito[TipoMovimento] = "Giacenza" &&<br>FACT_ContoDeposito[DataMovimento] = UltimaGiacenzaPrecedente<br>)<br>\|\|<br>(<br>FACT_ContoDeposito[TipoMovimento] <> "Giacenza" &&<br>FACT_ContoDeposito[DataMovimento] >= InizioPeriodo &&<br>(<br>FACT_ContoDeposito[DataMovimento] < DataCorrente \|\|<br>(<br>FACT_ContoDeposito[DataMovimento] = DataCorrente &&<br>FACT_ContoDeposito[EntryNo] <= OrdineCorrente<br>)<br>)<br>)<br>)<br><br>VAR Risultato = CALCULATE([Qta Magazzino CD], Tab_Filtro)<br><br>RETURN<br>IF(<br>HASONEVALUE(FACT_ContoDeposito[DataMovimento]),<br>Risultato,<br>BLANK()<br>)` |
| Qta Magazzino CD Giacenza IPEM | Quantità di giacenza iniziale/set-point per deposito IPEM in CD | `// Quantità registrata come set-point di giacenza per il deposito IPEM<br>CALCULATE (<br>[Qta Magazzino CD],<br>FACT_ContoDeposito[TipoMovimento] = "Giacenza" &&<br>FACT_ContoDeposito[CodiceDeposito] = "IPEM"<br>)` |
| Valore Magazzino | Valorizzazione giacenza al CMP cumulato | `// Valorizzazione della giacenza progressiva al CMP cumulato di acquisto<br>[Qta Magazzino Progressivo] * [CMP Acquisto CUM]` |
| Valore Magazzino (2) | Valorizzazione giacenza al CMP cumulato prodotto | `// Valorizzazione della giacenza progressiva al CMP cumulato di prodotto<br>[Qta Magazzino Progressivo] * [CMP Prodotto CUM]` |
| Rapporto Inventario | Indice di rotazione: Rapporto tra vendite N giorni e valore inventario | `// Indice di rotazione o copertura: Rapporto tra vendite ultimi N giorni e valore inventario attuale<br>DIVIDE([Importo Vendita N DAYS], [Importo Inventario], 0)` |
| Importo Dare (CG) | Somma degli importi 'Dare' (positivi) in CG | `// Somma degli importi positivi (Dare) nei movimenti di Contabilità Generale<br>CALCULATE(<br>SUMX ( FACT_MovimentiCG, FACT_MovimentiCG[Importo]),<br>FACT_MovimentiCG[Importo] > 0<br>)` |
| Unità Misura | Unità di misura di magazzino | `// Restituisce l'unità di misura utilizzata nei movimenti di magazzino<br>MINX(FACT_Magazzino, FACT_Magazzino[UnitàMisura])` |
| Qta Magazzino Progressivo YTD | Saldo quantità progressivo dall'inizio dell'anno corrente | `// Giacenza progressiva calcolata dall'inizio dell'anno (Year-To-Date)<br>CALCULATE (<br>[Qta Magazzino],<br>FILTER (<br>ALL ( DIM_Calendario ),<br>DIM_Calendario[Year] = MAX ( DIM_Calendario[Year] ) &&<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| Qta Magazzino Giacenza | Giacenza calcolata come Iniziale + Acquisti - Vendite | `// Calcolo teorico giacenza: Iniziale + Acquisti - Vendite<br>[Qta Giacenza Iniziale] + [Qta Acquisti] - [Qta Vendite]` |
| Qta Magazzino Giacenza OLD | Giacenza progressiva senza filtri gruppo vendita | `// Quantità progressiva ignorando i filtri sui gruppi vendita<br>CALCULATE(<br>[Qta Magazzino Progressivo],<br>REMOVEFILTERS(DIM_GruppiVendita)<br>)` |
| Qta Magazzino Prevista | Quantità magazzino prevista basata su giacenza 2021 | `// Stima della giacenza prevista partendo dalla giacenza iniziale del 2021<br>VAR Gruppo = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto] )<br>VAR DataCorrente = SELECTEDVALUE ( DIM_Calendario[Date] )<br><br>VAR QtaIniziale =<br>SUMX (<br>FILTER (<br>FACT_Giacenza,<br>FACT_Giacenza[Date] = DATE ( 2021, 1, 1 ) &&<br>FACT_Giacenza[GruppoProdotto] = Gruppo<br>),<br>FACT_Giacenza[Quantity]<br>)<br>VAR TabellaFiltroCalendario =<br>FILTER (<br>ALLSELECTED ( DIM_Calendario ),<br>DIM_Calendario[Date] < MAX ( DIM_Calendario[Date] )<br>)<br>RETURN<br>CALCULATE (<br>IF (<br>DataCorrente = DATE ( 2021, 1, 1 ) \|\| ISBLANK ( [Qta Magazzino] ),<br>QtaIniziale,<br>[Qta Magazzino]<br>),<br>TabellaFiltroCalendario<br>)` |
| Qta Magazzino Giacenza LM | Giacenza al mese precedente (Last Month) | `// Giacenza calcolata allo stesso giorno del mese precedente<br>CALCULATE ( [Qta Magazzino Giacenza OLD], DATEADD ( DIM_Calendario[Date], -1, MONTH ) )` |
| Qta Magazzino Giacenza IPEM | Giacenza calcolata per il deposito IPEM | `// Giacenza filtrata per il solo deposito IPEM<br>CALCULATE(<br>[Qta Magazzino Giacenza OLD],<br>FACT_Magazzino[CodiceDeposito] = "IPEM"<br>)` |
| Qta Magazzino CD per Movimento | Quantità CD dinamica in base al tipo movimento (Vendita vs Altri) | `// Determina se mostrare la quantità vendita o la quantità CD in base al tipo movimento selezionato<br>VAR MovimentoCorrente = SELECTEDVALUE(FACT_ContoDeposito[TipoMovimento], 0)<br>RETURN<br>IF(MovimentoCorrente = "Vendita", [Qta Vendita Deposito], [Qta Magazzino CD])` |
| Qta Vendita Deposito | Quantità venduta per deposito mappato su soggetti CD | `// Calcola le quantità vendute dai depositi, mappando i depositi CD sui codici soggetti (C00*)<br>VAR Gruppo = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () )<br>VAR Deposito = SELECTEDVALUE ( FACT_ContoDepositoOLD[CodiceDeposito], BLANK () )<br>VAR SoggettoFiltro =<br>SWITCH (<br>TRUE (),<br>Deposito = "AUTOGASNORD", "C00525",<br>Deposito = "BUTANGAS", "C00229",<br>Deposito = "LIQUIGAS", "C00261",<br>Deposito = "VERSALIS", "C00794",<br>""<br>)<br><br>VAR QtaMix =<br>CALCULATE(<br>SUMX(FACT_Vendite, FACT_Vendite[Quantity]),<br>FACT_Vendite[CodiceArticolo] IN {"1","1T"} &&<br>IF( SoggettoFiltro <> "",<br>FACT_Vendite[CodiceSoggetto] = SoggettoFiltro,<br>FACT_Vendite[CodiceCategoria] <> "PASSAGGIO"<br>)<br>)<br><br>VAR QtaPropano =<br>CALCULATE(<br>SUMX(FACT_Vendite, FACT_Vendite[Quantity]),<br>FACT_Vendite[CodiceArticolo] IN {"2","2T"} &&<br>IF( SoggettoFiltro <> "",<br>FACT_Vendite[CodiceSoggetto] = SoggettoFiltro,<br>FACT_Vendite[CodiceCategoria] <> "PASSAGGIO"<br>)<br>)<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>Gruppo = "MIX", QtaMix,<br>Gruppo = "PROPANO", QtaPropano,<br>QtaMix + QtaPropano<br>)` |
| Qta Magazzino Giacenza (2) | Giacenza specifica 2024 con set-point iniziale al 01/01 | `// Calcolo giacenza 2024 basato su set-point iniziale e movimenti YTD<br>VAR Prodotto = SELECTEDVALUE(DIM_Articoli[GruppoProdotto])<br>VAR DataInizio2024 = DATE(2024,1,1)<br><br>VAR GiacenzaInizialePropano = 4901<br>VAR GiacenzaInizialeMix = 219.520<br>VAR GiacenzaIniziale = IF(Prodotto = "PROPANO", GiacenzaInizialePropano, GiacenzaInizialeMix)<br><br>VAR QtaAcquistoYTD = TOTALYTD ( [Qta Acquisti], DIM_Calendario[Date] )<br>VAR QtaVenditaYTD = TOTALYTD ( [Qta Vendita Prodotto], DIM_Calendario[Date] )<br><br>RETURN<br>IF(<br>MIN(DIM_Calendario[Date]) = DataInizio2024 && MAX(DIM_Calendario[Date]) = DATE(2024,1,31),<br>GiacenzaIniziale,<br>GiacenzaIniziale + QtaAcquistoYTD - QtaVenditaYTD<br>)` |
| Qta Magazzino Vendita (MAG) | Quantità scaricata da FACT_Magazzino per vendite | `// Quantità scaricata dal magazzino per movimenti di tipo 'Vendita'<br>CALCULATE(<br>[Qta Magazzino],<br>FACT_Magazzino[TipoMovimento] = "Vendita"<br>)` |
| Qta Acquisti Progressivo | Quantità acquisti accumulata nel tempo (periodo corrente) | `// Quantità totale acquistata accumulata nel periodo selezionato<br>IF (<br>ISBLANK ( [Qta Acquisti] ),<br>BLANK (),<br>CALCULATE (<br>[Qta Acquisti],<br>FILTER (<br>ALLSELECTED ( DIM_Calendario ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)<br>)` |
| Qta Magazzino Acquisto | Carichi di magazzino (Acquisti + Rettifiche Positive) | `// Quantità caricata a magazzino per Acquisti o Rettifiche Positive<br>CALCULATE(<br>[Qta Magazzino],<br>FACT_Magazzino[TipoMovimento] IN {"Acquisto", "Rettifica Positiva"}<br>)` |
| Mostra Giacenza No Movimenti | Flag per visualizzazione righe di giacenza senza movimenti nel periodo | `// Flag tecnico per gestire la visualizzazione delle giacenze anche in assenza di movimenti nel periodo<br>VAR HaQuantita = NOT(ISBLANK([Qta Magazzino CD]))<br>VAR AnnoCorrente = YEAR(MAX(FACT_ContoDeposito[DataMovimento]))<br>VAR InizioAnno = DATE(AnnoCorrente, 1, 1)<br>VAR IsGiacenzaIniziale =<br>MAX(FACT_ContoDeposito[TipoMovimento]) = "Giacenza" &&<br>MAX(FACT_ContoDeposito[DataMovimento]) <= InizioAnno<br>VAR IsAltroMovimento = MAX(FACT_ContoDeposito[TipoMovimento]) <> "Giacenza"<br><br>RETURN<br>IF(<br>HaQuantita && (IsGiacenzaIniziale \|\| IsAltroMovimento),<br>1,<br>BLANK()<br>)` |
| FiltroAttivo | Stato dei filtri principali (Debug) | `// Visualizza lo stato dei filtri attivi per Gruppo e Categoria (debug)<br>VAR IsGruppoFiltrato = ISFILTERED(DIM_GruppiVendita[Gruppo])<br>VAR IsCategoriaFiltrata = ISFILTERED(FACT_MovimentiCG[CodiceCategoria])<br>RETURN<br>"Gruppo: " & IF(IsGruppoFiltrato, "✅", "❌") &<br>" \| Categoria: " & IF(IsCategoriaFiltrata, "✅", "❌")` |
| Qta Giacenza Iniziale | Alias quantità giacenza iniziale IPEM | `// Alias per la giacenza iniziale IPEM in Conto Deposito<br>[Qta Magazzino CD Giacenza IPEM]` |

## KPI_Mix

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| # Tassi Mese | Numero di tassi di cambio rilevati nel mese | `// Conta il numero di rilevazioni di tassi di cambio nel periodo selezionato<br>COUNTROWS ( ALLSELECTED ( FACT_Cambi ) )` |
| Tasso Giorno | Tasso di cambio giornaliero (USD/EUR) con logica di fallback | `// Determina il tasso di cambio USD/EUR per la data di spedizione dell'ordine<br>// Se il tasso non è presente per il giorno esatto, prova a recuperare quello di 3 giorni dopo (fall-back)<br>VAR DataOrdine = SELECTEDVALUE ( FACT_Ordini[DataSpedizione] )<br>VAR TassoEsatto =<br>CALCULATE (<br>SELECTEDVALUE ( FACT_Cambi[Tasso] ),<br>FACT_Cambi[Data] = DataOrdine<br>)<br>VAR TassoFallback =<br>CALCULATE (<br>SELECTEDVALUE ( FACT_Cambi[Tasso] ),<br>FACT_Cambi[Data] = DataOrdine + 3<br>)<br>RETURN<br>IF ( TassoEsatto <> 0 && NOT(ISBLANK(TassoEsatto)), TassoEsatto, TassoFallback )` |
| Tasso Medio Mese | Tasso di cambio medio mensile | `// Calcola la media aritmetica semplice dei tassi di cambio del periodo<br>VAR NrGiorni = COUNTROWS ( FACT_Cambi )<br>VAR SommaTassi = CALCULATE ( SUMX ( FACT_Cambi, FACT_Cambi[Tasso] ), FACT_Cambi )<br>RETURN<br>DIVIDE ( SommaTassi, NrGiorni, 0 )` |
| Prezzo USD | Prezzo unitario in USD dalla tabella prezzi Mix | `// Recupera il prezzo in USD dalla tabella dei prezzi Mix filtrando per cliente e periodo<br>VAR ClienteCodice = SELECTEDVALUE ( DIM_Clienti[CodiceCliente] )<br>VAR Anno = SELECTEDVALUE ( DIM_Calendario[Year] )<br>VAR Mese = SELECTEDVALUE ( DIM_Calendario[MonthName] )<br><br>VAR TabellaFiltrata =<br>FILTER (<br>FACT_PrezziMix,<br>'FACT_PrezziMix'[Cliente] = ClienteCodice &&<br>(FORMAT(YEAR(FACT_PrezziMix[Data]), "String") = Anno \|\| MONTH(FACT_PrezziMix[Data]) = Mese)<br>)<br>RETURN<br>SELECTCOLUMNS ( TabellaFiltrata, "Prezzo", FACT_PrezziMix[Prezzo USD] )` |
| # ATB Giorno | Numero di ordini giornalieri | `// Conta il numero di ordini effettuati nel giorno (ATB)<br>COUNT ( FACT_Ordini[CodiceOrdine] )` |
| Prezzo Euro | Prezzo in Euro calcolato in base al cliente (MITSUI tasso giorno, TOTAL tasso mese) | `// Converte il prezzo da USD a EUR in base alle regole specifiche del cliente:<br>// TOTAL: usa tasso medio mese<br>// MITSUI: usa tasso del giorno<br>VAR Cliente = SELECTEDVALUE ( FACT_Ordini[CodiceClienteFatturazione] )<br>VAR Deposito = SELECTEDVALUE ( FACT_Ordini[CodiceDeposito] )<br>VAR IsTotal = ( Cliente = "C00991" \|\| Deposito = "TOTAL" )<br>VAR IsMitsui = ( Cliente = "C01049" \|\| Deposito = "MITSUI" )<br>RETURN<br>IF (<br>IsTotal \|\| IsMitsui,<br>SWITCH (<br>TRUE (),<br>IsTotal, DIVIDE ( [Prezzo USD], [Tasso Medio Mese], 0 ),<br>IsMitsui, DIVIDE ( [Prezzo USD], [Tasso Giorno], 0 )<br>),<br>BLANK ()<br>)` |
| Qta BIL | Quantità BIL in tonnellate (da ordini ODA) | `// Calcola la quantità fatturata (BIL) in tonnellate filtrando per ordini ODA<br>// Converte automaticamente da Kg a Ton se il valore è >= 1000<br>CALCULATE(<br>SUMX(<br>FILTER(<br>FACT_Ordini,<br>LEFT(FACT_Ordini[CodiceOrdine], 3) = "ODA"<br>),<br>IF(FACT_Ordini[Quantity] >= 1000,<br>DIVIDE(FACT_Ordini[Quantity], 1000, 0),<br>FACT_Ordini[Quantity]<br>)<br>)<br>)` |
| Qta Resa | Quantità RESA in tonnellate (da ordini ODA) | `// Calcola la quantità resa (NetWeight) in tonnellate filtrando per ordini ODA<br>VAR QuantitaResa =<br>SUMX(<br>FILTER(<br>FACT_Ordini,<br>LEFT(FACT_Ordini[CodiceOrdine], 3) = "ODA"<br>),<br>IF(FACT_Ordini[NetWeight] >= 1000,<br>DIVIDE(FACT_Ordini[NetWeight], 1000, 0),<br>FACT_Ordini[NetWeight]<br>)<br>)<br>RETURN<br>QuantitaResa` |
| Importo USD | Importo totale in USD per il Mix | `// Calcola il valore totale degli ordini in USD: Prezzo unitario x Qta BIL<br>SUMX ( VALUES ( DIM_Calendario[MonthName] ), [Prezzo USD MIX] * [Qta BIL] )` |
| Accisa Unitario | Accisa unitaria per tonnellata | `// Somma delle accise unitarie registrate sugli ordini<br>SUMX ( FACT_Ordini, FACT_Ordini[Accisa])` |
| Importo Accisa | Importo totale accisa | `// Importo totale dell'accisa: Accisa Unitaria x Qta BIL<br>SUMX ( VALUES ( DIM_Calendario[MonthName] ), [Accisa Unitario] * [Qta BIL] )` |
| Trasporto Unitario | Costo di trasporto unitario per tonnellata in base alla logistica | `// Determina il costo di trasporto unitario per tonnellata in base alla base logistica<br>VAR Deposito = SELECTEDVALUE ( FACT_Ordini[CodiceDeposito] )<br>RETURN<br>SWITCH (<br>TRUE (),<br>Deposito IN { "MITSUI", "TOTAL" }, 0,<br>Deposito = "IPEM", 23,<br>0<br>)` |
| Importo Trasporto | Importo totale trasporto | `// Valore totale del trasporto: Costo Unitario x Qta BIL<br>[Trasporto Unitario] * [Qta BIL]` |
| Costo Totale | Costo totale (Importo Euro + Trasporto) | `// Somma dell'importo merce in Euro e dei costi di trasporto<br>[Importo Euro] + [Importo Trasporto]` |
| Costo Unitario | Costo unitario per tonnellata incl. trasporto | `// Rapporto tra costo totale (merce + trasporto) e quantità BIL<br>DIVIDE ( [Costo Totale], [Qta BIL], 0 )` |
| Nome Spedizioniere | Ragione sociale dello spedizioniere dell'ordine | `// Recupera il nome dello spedizioniere associato all'ordine corrente<br>CALCULATE (<br>FIRSTNONBLANK ( DIM_Spedizionieri[Name], 1 ),<br>FILTER (<br>DIM_Spedizionieri,<br>DIM_Spedizionieri[CodiceSpedizioniere] = SELECTEDVALUE ( FACT_Ordini[CodiceSpedizioniere] )<br>)<br>)` |
| Tasso Giorno Medio | Media dei tassi di cambio giornalieri nel periodo | `// Media dei tassi di cambio giornalieri nel contesto temporale<br>AVERAGEX ( VALUES ( DIM_Calendario[Date] ), [Tasso Giorno] )` |
| Costo €/T NEW | Costo per tonnellata (logica di aggregazione media) | `// Calcola il costo per tonnellata gestendo il contesto di singola data o aggregato<br>IF (<br>HASONEVALUE ( FACT_Ordini[DataSpedizione] ),<br>[Costo €/T],<br>AVERAGEX ( VALUES ( FACT_Ordini[DataSpedizione] ), [Costo €/T] )<br>)` |
| Prezzo USD Mese | Media mensile del prezzo in USD | `// Media del prezzo USD nel periodo selezionato<br>AVERAGEX ( VALUES ( FACT_Ordini[DataSpedizione] ), [Prezzo USD MIX] )` |
| Costo €/T Medio | Costo medio per tonnellata su base mensile | `// Media del costo per tonnellata aggregata per mese in calendario<br>AVERAGEX ( VALUES ( DIM_Calendario[MonthInCalendar] ), [Costo €/T] )` |
| Importo Euro | Importo totale in Euro per il Mix | `// Valore totale degli ordini in Euro: Prezzo unitario medio mese x Qta BIL<br>[Prezzo € MIX Mese] * [Qta BIL]` |
| Costo MIX | Costo di riferimento per il prodotto Mix | `// Restituisce il valore della misura base [Costo Mix]<br>[Costo Mix]` |
| Prezzo USD MIX | Prezzo medio USD dalla tabella prezzi Mix | `// Media aritmetica dei prezzi USD presenti nella tabella FACT_PrezziMix<br>AVERAGEX ( FACT_PrezziMix, FACT_PrezziMix[Prezzo USD] )` |
| Costo MIX 2 | Costo unitario MIX filtrato per clienti strategici | `// Costo per tonnellata filtrato per i clienti principali (TOTAL/MITSUI) e prodotti MIX (1, 1T)<br>CALCULATE (<br>[Costo €/T],<br>FILTER (<br>FACT_Ordini,<br>FACT_Ordini[CodiceClienteFatturazione] IN { "C00991", "C01049" } &&<br>FACT_Ordini[CodiceArticolo] IN { "1", "1T" }<br>)<br>)` |
| Prezzo € MIX | Prezzo MIX convertito in Euro (tasso specifico cliente) | `// Conversione del prezzo USD in Euro utilizzando il tasso specifico del cliente<br>DIVIDE ( [Prezzo USD MIX], [Tasso Giorno Cliente], 0 )` |
| Prezzo € MIX Mese | Prezzo medio Euro MIX per cliente | `// Media ponderata del prezzo Euro MIX aggregata per cliente<br>AVERAGEX (<br>VALUES(DIM_Clienti[Nome & Codice]),<br>[Prezzo € MIX]<br>)` |
| Costo Totale Mese | Costo totale mensile aggregato per giorno | `// Somma dei costi totali giornalieri per l'intero mese<br>SUMX ( VALUES ( FACT_Ordini[DataSpedizione] ), [Costo Totale] )` |
| Tasso Giorno Medio 2 | Tasso di cambio medio con logica di contesto (Mese vs Totale) | `// Logica dinamica: se nel contesto c'è un solo mese usa la media giornaliera, altrimenti la media mensile<br>IF ( HASONEVALUE ( DIM_Calendario[MonthName] ), [Tasso Giorno Medio], [Tasso Medio Mese] )` |
| Tasso Mese 2 | Tasso medio mensile ricalcolato con filtri espliciti | `// Calcolo manuale del tasso medio mensile filtrato per l'anno e mese selezionati<br>VAR AnnoCorrente = VALUE({ SELECTEDVALUE ( DIM_Calendario[Year] ) })<br>VAR MeseCorrente = VALUE({ SELECTEDVALUE ( DIM_Calendario[MonthnYear] ) })<br><br>VAR ConteggioGiorni =<br>CALCULATE (<br>COUNTROWS ( ALLSELECTED ( FACT_Cambi[Data] ) ),<br>YEAR(FACT_Cambi[Data]) = AnnoCorrente &&<br>MONTH( FACT_Cambi[Data] ) = MeseCorrente<br>)<br>VAR SommaTassi =<br>CALCULATE ( SUMX ( FACT_Cambi, FACT_Cambi[Tasso] ), ALLSELECTED ( FACT_Cambi ) )<br><br>RETURN<br>DIVIDE(SommaTassi, ConteggioGiorni, 0)` |
| Tasso Giorno Cliente | Tasso applicabile al cliente (Mese per TOTAL, Giorno per MITSUI) | `// Determina quale tasso applicare al cliente selezionato:<br>// TOTAL: Tasso Medio Mese<br>// MITSUI: Tasso Giorno Medio<br>VAR CodiceCliente = SELECTEDVALUE ( DIM_Clienti[CodiceCliente] )<br>VAR CodiceDeposito = SELECTEDVALUE ( DIM_Deposito[CodiceDeposito] )<br>VAR IsTotal = ( CodiceCliente = "C00991" \|\| CodiceDeposito = "TOTAL" )<br>VAR IsMitsui = ( CodiceCliente = "C01049" \|\| CodiceDeposito = "MITSUI" )<br><br>RETURN<br>IF (<br>IsTotal \|\| IsMitsui,<br>SWITCH (<br>TRUE (),<br>IsTotal, [Tasso Medio Mese],<br>IsMitsui, [Tasso Giorno Medio]<br>),<br>BLANK ()<br>)` |
| # Qta_BIL | Conteggio record ordini ODA | `COUNT(FACT_Ordini[CodiceOrdine])` |
| Qta BIL (2) | Quantità BIL totale per ordini ODA | `// Somma grezza delle quantità per gli ordini di tipo ODA<br>CALCULATE(<br>SUMX(FACT_Ordini, FACT_Ordini[Quantity]),<br>LEFT(FACT_Ordini[CodiceOrdine], 3) = "ODA"<br>)` |
| Spedizionieri Attivi | Spedizionieri con movimenti MIX nel periodo | `// Restituisce il nome dello spedizioniere se ha effettuato almeno una consegna di prodotti MIX (1, 1T)<br>IF(<br>CALCULATE(<br>COUNTROWS(FACT_Ordini),<br>FACT_Ordini[CodiceArticolo] IN {"1", "1T"}<br>) > 0,<br>SELECTEDVALUE(DIM_Spedizionieri[Name])<br>)` |
| Ordini MIX | Numero ordini MIX clienti TOTAL/MITSUI | `// Conta gli ordini di prodotti MIX effettuati dai clienti strategici<br>CALCULATE(<br>COUNTROWS(FACT_Ordini),<br>FACT_Ordini[CodiceArticolo] IN {"1", "1T"} &&<br>FACT_Ordini[CodiceClienteVendita] IN {"C00991", "C01049"}<br>)` |
| Qta Acquisto MIX Tempa | Quantità acquisto MIX (Tab Tempa) | `// Somma delle quantità di acquisto MIX dalla tabella temporanea Tempa<br>SUMX(FACT_AcquistiMixTemp, FACT_AcquistiMixTemp[Qta])` |
| # Qta Acquisto MIX Tempa | Numero carichi MIX (Tab Tempa) | `// Conteggio dei record di acquisto MIX nella tabella temporanea Tempa<br>COUNTX(FACT_AcquistiMixTemp, FACT_AcquistiMixTemp[Qta])` |

## KPI_ObiettivoVendite

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Valore Obiettivo Vendite | Valore obiettivo vendite (da parametro) | `// Restituisce il valore dell'obiettivo di vendita selezionato dal parametro<br>SELECTEDVALUE('KPI_ObiettivoVendite'[Obiettivo Vendite])` |

## KPI_Prodotto

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Agenzia | Spese di agenzia (CG 4030004 + Accantonamenti) | `// Somma i costi di agenzia registrati in CG (conto 4030004) e gli accantonamenti associati<br>VAR ImportoCG =<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "4030004" && FACT_MovimentiCG[CodiceSorgente] = "ACQUISTI"<br>)<br>VAR ImportoAccantonamenti =<br>CALCULATE (<br>[Importo Accantonamenti],<br>FACT_Accantonamenti[CodiceConto] = "4030004"<br>)<br>RETURN<br>ImportoCG + ImportoAccantonamenti` |
| Controstallie | Oneri per controstallie (CG 4030008, Progetto ML04) | `// Costi per controstallie registrati su conto 4030008 e progetto ML04<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "4030008" && FACT_MovimentiCG[Progetto] = "ML04"<br>)` |
| Dogana | Spese doganali (CG 4030002, Sorgente ACQUISTI) | `// Oneri doganali registrati su conto 4030002 con sorgente ACQUISTI<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "4030002" && FACT_MovimentiCG[CodiceSorgente] = "ACQUISTI"<br>)` |
| Perizia | Spese per perizie (CG 4030007, Progetto ML04) | `// Spese per perizie tecniche registrate su conto 4030007 e progetto ML04<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "4030007" && FACT_MovimentiCG[Progetto] = "ML04"<br>)` |
| Recupero Controstallie | Ricavi da recupero controstallie (CG 3400025) | `// Ricavi da recupero controstallie registrati su conto 3400025<br>CALCULATE ( [Importo], FACT_MovimentiCG[CodiceConto] = "3400025" )` |
| Recupero Dazi Doganali | Recupero dazi doganali da documenti di vendita | `// Recupero dei dazi doganali basato su documenti di vendita (Articolo 3400026 o Progetto ML04)<br>VAR NaveSelezionata = SELECTEDVALUE ( DIM_MezziTrasporto[CodiceMezzo] )<br>VAR ImportoRecupero =<br>CALCULATE (<br>[Importo Doc Vendita],<br>'FACT_DocumentoVendita'[CodiceArticolo] = "3400026" \|\|<br>'FACT_DocumentoVendita'[Progetto] = "ML04" \|\|<br>'FACT_DocumentoVendita'[MezzoTrasporto] = NaveSelezionata<br>)<br>RETURN<br>ImportoRecupero` |
| Recupero Spese Terzi | Ricavi recupero spese terzi (CG 3400023) | `// Ricavi per recupero spese effettuate per conto terzi (Conto 3400023)<br>CALCULATE ( [Importo], FACT_MovimentiCG[CodiceConto] = "3400023" )` |
| Rimorchio | Spese di rimorchio (CG 4030006, Progetto ML04) | `// Costi per servizi di rimorchio (Conto 4030006, Progetto ML04)<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "4030006" && FACT_MovimentiCG[Progetto] = "ML04"<br>)` |
| Trasporto Acquisti | Costi trasporto acquisti (CG 4030003, Sorgente REGACQ) | `// Costi di trasporto su acquisti registrati su conto 4030003<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "4030003" && FACT_MovimentiCG[CodiceSorgente] = "REGACQ"<br>)` |
| COSTI_ACCESSORI_GPL | Totale costi accessori acquisto GPL (Costi - Recuperi) | `// Somma algebrica di tutti i costi accessori legati all'acquisto di GPL (Dogana, Agenzia, Rimorchio, etc.) al netto dei recuperi<br>VAR SommaCosti = [Dogana] + [Agenzia] + [Rimorchio] + [Perizia] + [Controstallie]<br>VAR SommaRecuperi = [Recupero Spese Terzi] + [Recupero Controstallie] + [Recupero Dazi Doganali]<br>RETURN<br>SommaCosti - SommaRecuperi` |
| €/Ton CostoGPL_Resa | Costo totale nave per tonnellata resa | `// Incidenza unitaria per tonnellata del costo totale nave sulla quantità resa IPEM<br>DIVIDE ( [Total Costs NAVI], [QuantityIPEM], 0 )` |
| COSTO GPL | Costo base GPL (Materia Prima + Trasporto) | `// Somma dei costi di materia prima e trasporto acquisti<br>[Materia Prima €] + [Trasporto Acquisti]` |
| Materia Prima € OLD | Costo materia prima (legacy via accantonamenti) | `// Misura legacy per il calcolo della materia prima basato su accantonamenti<br>VAR ImpMP =<br>CALCULATE (<br>[Importo Accantonamenti],<br>FACT_Accantonamenti[CodiceConto] = "4020002"<br>)<br>VAR ImpMP2 =<br>CALCULATE (<br>[Importo Accantonamenti],<br>FACT_Accantonamenti[CodiceConto] = "2030004"<br>)<br>RETURN<br>ImpMP + ImpMP2` |
| Total Costs NAVI | Costo totale navi (GPL + Accessori) | `// Costo complessivo associato alle navi: GPL + Accessori<br>[COSTO GPL] + [COSTI_ACCESSORI_GPL]` |
| Materia Prima € | Valore materia prima da Contabilità Generale | `// Calcola il valore della materia prima dai movimenti CG (Conti 4020002 e 2030004)<br>VAR ImportoMP1 =<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "4020002" && FACT_MovimentiCG[CodiceSorgente] = "REGACQ"<br>)<br>VAR ImportoMP2 =<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "2030004" &&<br>FACT_MovimentiCG[CodiceSorgente] IN { "REGACQ", "REGACQIC" } &&<br>FACT_MovimentiCG[Progetto] = ""<br>)<br>RETURN<br>ImportoMP1 + ImportoMP2` |
| Materia Prima € 2 | Materia prima filtrata per record con mezzo trasporto | `// Altra versione del calcolo materia prima filtrata per mezzo di trasporto<br>CALCULATE (<br>[Importo],<br>FACT_MovimentiCG[CodiceConto] = "2030004"<br>&& FACT_MovimentiCG[TipoSorgente] = ""<br>&& FACT_MovimentiCG[MezzoTrasporto] <> ""<br>)` |
| Perizia 2 | Spese perizia (versione ACQ) | `// Versione alternativa della misura Perizia utilizzando [Importo ACQ]<br>CALCULATE (<br>[Importo ACQ],<br>FACT_MovimentiCG[CodiceConto] = "4030007" && FACT_MovimentiCG[Progetto] = "ML04"<br>)` |
| Costo Acquisto Qta Ton | Costo di acquisto unitario (CMP) nel contesto quantità | `// Restituisce il CMP di acquisto solo se è presente una quantità nel contesto<br>IF ( ISBLANK ( [Quantity] ), BLANK (), [CMP Acquisto] )` |
| Recupero Spese Trasporto | Recupero spese di trasporto (CG 3400024) | `// Ricavi da recupero spese di trasporto (Conto 3400024)<br>CALCULATE ( ABS ( [Importo] ), FACT_MovimentiCG[CodiceConto] = "3400024" )` |
| CMP Acquisto CUM | CMP Acquisto cumulato nel tempo | `// Calcola il CMP di acquisto accumulato storicamente<br>CALCULATE (<br>[CMP Acquisto],<br>FILTER ( ALL ( DIM_Calendario ), DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] ) )<br>)` |
| CMP Prodotto | CMP calcolato sulla quantità totale | `// Rapporto tra i costi totali nave e la quantità totale prodotta/acquistata<br>DIVIDE ( [Total Costs NAVI], [Quantity] )` |
| CMP Acquisto CD | CMP calcolato sulla quantità resa CD | `// Rapporto tra i costi totali nave e la quantità resa in Conto Deposito<br>DIVIDE ( [Total Costs NAVI], [QuantityIPEM] )` |
| CMP Prodotto CUM | CMP Prodotto cumulato nel tempo | `// Calcola il CMP prodotto accumulato storicamente<br>CALCULATE (<br>[CMP Prodotto],<br>FILTER ( ALL ( DIM_Calendario ), DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] ) )<br>)` |
| CMP Acquisto | CMP di acquisto dinamico per MIX, PROPANO o totale ponderato | `// Calcola il CMP di acquisto differenziato per gruppo prodotto (MIX vs PROPANO)<br>// Se nessun gruppo è filtrato, esegue una media ponderata tra i due prodotti<br>VAR GruppoFiltrato = { SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () ) }<br><br>VAR CMP_Mix = [Costo Mix Resa]<br>VAR CMP_Propano = [Costo Propano]<br><br>VAR Qta_Mix = SUMX(FACT_Mix, FACT_Mix[Qta BIL] )<br>VAR Qta_Propano = SUMX(FACT_Propano, FACT_Propano[QuantityIPEM] )<br><br>VAR CMP_Ponderato = DIVIDE((CMP_Mix * Qta_Mix) + (CMP_Propano * Qta_Propano), Qta_Mix + Qta_Propano)<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>GruppoFiltrato = "MIX", CMP_Mix,<br>GruppoFiltrato = "PROPANO", CMP_Propano,<br>GruppoFiltrato = BLANK (), CMP_Ponderato<br>)` |
| CMP Acquisto Prodotto | Rapporto tra Importo Acquisto e Qta Acquisto | `// Rapporto semplice tra importo totale acquisti e quantità acquistata<br>DIVIDE ( [Importo Acquisto], [Qta Acquisto], 0 )` |
| Importo Costo Acquisto | Valore acquisto (QuantityIPEM x CMP) | `// Valorizzazione della quantità resa CD al CMP di acquisto<br>[QuantityIPEM] * [CMP Acquisto]` |
| Importo Acquisto | Importo totale acquisti per gruppo prodotto | `// Somma gli importi di acquisto per MIX e PROPANO in base al contesto<br>VAR GruppoFiltrato = { SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () ) }<br>VAR Imp_Mix = [Importo Costo Mix]<br>VAR Imp_Propano = [Importo Costo Propano]<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>GruppoFiltrato = "MIX", Imp_Mix,<br>GruppoFiltrato = "PROPANO", Imp_Propano,<br>GruppoFiltrato = BLANK (), Imp_Mix + Imp_Propano<br>)` |
| Margine Unitario | Differenza unitaria tra Prezzo Medio e CMP | `// Differenza tra prezzo medio di vendita e CMP di acquisto<br>VAR Prezzo = [Prezzo Medio Vendita]<br>VAR Costo = [CMP Acquisto]<br>RETURN<br>IF (Prezzo > 0, Prezzo - Costo, 0)` |
| % Profitto | Percentuale di profitto sul fatturato | `// Rapporto tra importo margine e importo vendita<br>DIVIDE ( [Importo Margine], [Importo Vendita], 0 )` |
| Trasporto Vendite | Spese trasporto su vendite (CG 4110045) | `// Costi di trasporto legati alle vendite (Conto 4110045)<br>CALCULATE ( [Importo], FACT_MovimentiCG[CodiceConto] = "4110045" )` |
| Filtro Anno | Valore dell'anno selezionato | `// Restituisce l'anno selezionato nel filtro calendario<br>SELECTEDVALUE ( DIM_Calendario[Year] )` |
| Filtro Mese | Valore del mese selezionato | `// Restituisce il nome del mese selezionato nel filtro calendario<br>SELECTEDVALUE ( DIM_Calendario[MonthName] )` |
| Costo Venduto | Costo del venduto basato su CMP Giacenza | `// Calcola il costo del venduto: Giacenza CMP x Quantità Venduta<br>[CMP Giacenza] * [Qta Vendita]` |
| Importo Giacenza Iniziale | Importo rimanenze iniziali (CG 401*) | `// Valore delle giacenze iniziali (Conti 4010002 per Propano, 4010003 per Mix)<br>VAR GruppoFiltrato = { SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () ) }<br><br>VAR Imp_Mix = CALCULATE ( [Importo], FACT_MovimentiCG[CodiceConto] = "4010003" )<br>VAR Imp_Propano = CALCULATE ( [Importo], FACT_MovimentiCG[CodiceConto] = "4010002" )<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>GruppoFiltrato = "MIX", Imp_Mix,<br>GruppoFiltrato = "PROPANO", Imp_Propano,<br>GruppoFiltrato = BLANK (), Imp_Mix + Imp_Propano<br>)` |
| CMP Giacenza | CMP calcolato sulla somma di acquisti e rimanenze iniziali | `// Calcolo del CMP di giacenza come media ponderata tra (Acquisti + Iniziali)<br>VAR ValoreTotale = [Importo Acquisto] + [Importo Giacenza Iniziale]<br>VAR QuantitaTotale = [Qta Acquisto] + [Qta Giacenza Iniziale]<br>RETURN<br>DIVIDE(ValoreTotale, QuantitaTotale)` |
| CMP Acquisto AVG QTD | Media trimestrale del CMP Acquisto | `// Media del CMP di acquisto calcolata sui trimestri (Quarter-To-Date)<br>AVERAGEX (<br>VALUES(DIM_Calendario[MonthInCalendar]),<br>TOTALQTD([CMP Acquisto], DIM_Calendario[Date])<br>)` |
| CMP Acquisto AVG YTD | Media annuale progressiva del CMP Acquisto | `// Media del CMP di acquisto calcolata dall'inizio dell'anno (Year-To-Date)<br>AVERAGEX (<br>VALUES(DIM_Calendario[MonthInCalendar]),<br>TOTALYTD([CMP Acquisto], DIM_Calendario[Date])<br>)` |
| CMP Giacenza YTD | Media mobile a 4 mesi del CMP YTD | `// Calcola la media dei CMP YTD degli ultimi 4 mesi<br>VAR MediaYTD = [CMP Acquisto AVG YTD]<br>VAR CMP_LM1 = CALCULATE ( MediaYTD, DATEADD ( DIM_Calendario[Date], -1, MONTH ) )<br>VAR CMP_LM2 = CALCULATE ( MediaYTD, DATEADD ( DIM_Calendario[Date], -2, MONTH ) )<br>VAR CMP_LM3 = CALCULATE ( MediaYTD, DATEADD ( DIM_Calendario[Date], -3, MONTH ) )<br>VAR CMP_LM4 = CALCULATE ( MediaYTD, DATEADD ( DIM_Calendario[Date], -4, MONTH ) )<br><br>VAR SommaCMP = CMP_LM1 + CMP_LM2 + CMP_LM3 + CMP_LM4<br>RETURN<br>DIVIDE(SommaCMP, 4, 0)` |
| CMP Acquisto 2 | CMP Acquisto basato su medie tabelle FACT_Mix/Propano | `// Media aritmetica semplice dei costi medi MIX e PROPANO<br>VAR GruppoFiltrato = { SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () ) }<br>VAR CMP_Mix = AVERAGEX(FACT_Mix, FACT_Mix[Costo] )<br>VAR CMP_Propano = AVERAGEX(FACT_Propano, FACT_Propano[Costo] )<br>RETURN<br>SWITCH (<br>TRUE (),<br>GruppoFiltrato = "MIX", CMP_Mix,<br>GruppoFiltrato = "PROPANO", CMP_Propano,<br>DIVIDE(CMP_Mix + CMP_Propano, 2, 0)<br>)` |
| Importo Margine | Margine totale (Qta x Margine Unitario) | `// Margine totale calcolato come Quantità Venduta x Margine Unitario<br>[Qta Vendita] * [Margine Unitario]` |
| # Mezzi | Conteggio mezzi di trasporto (escluso ATB) | `// Conta il numero di mezzi di trasporto diversi da 'ATB'<br>CALCULATE(<br>COUNTX(DIM_MezziTrasporto, [QuantityIPEM]),<br>DIM_MezziTrasporto[CodiceMezzo] <> "ATB"<br>)` |
| Costo Venduto 2 | Delta tra giacenza iniziale e acquisti | `// Calcolo alternativo del costo venduto (Differenza tra Iniziali e Acquisti - logica specifica)<br>[Importo Giacenza Iniziale] - [Importo Acquisto]` |
| Costo Venduto 3 | Costo del venduto calcolato per differenza inventariale (Iniziale + Acquisti - Finale) | `// Calcola il costo del venduto basato sulla formula: Giacenza Iniziale + Acquisti - Giacenza Finale<br>VAR Imp_Giacenza = [Importo Giacenza Iniziale]<br>VAR Imp_Giacenza_LM = CALCULATE ( Imp_Giacenza, DATEADD ( DIM_Calendario[Date], -1, MONTH ) )<br>VAR Imp_Giacenza_NM = CALCULATE ( Imp_Giacenza, DATEADD ( DIM_Calendario[Date], 1, MONTH ) )<br>VAR Imp_Acquisto_CM = [Importo Acquisto]<br><br>RETURN<br>Imp_Giacenza_LM + Imp_Acquisto_CM - Imp_Giacenza_NM` |
| CMP Acquisto MEDIAN YTD | Mediana annuale progressiva del CMP Acquisto | `// Mediana del CMP di acquisto calcolata dall'inizio dell'anno (YTD)<br>MEDIANX(<br>VALUES(DIM_Calendario[MonthInCalendar]),<br>TOTALYTD([CMP Acquisto], DIM_Calendario[Date])<br>)` |
| Importo Giacenza Finale | Importo rimanenze finali (CG 330*) | `// Valore delle giacenze finali (Conti 3300002 per Propano, 3300003 per Mix)<br>VAR GruppoFiltrato = { SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () ) }<br><br>VAR Imp_Mix = CALCULATE ( [Importo], FACT_MovimentiCG[CodiceConto] = "3300003" )<br>VAR Imp_Propano = CALCULATE ( [Importo], FACT_MovimentiCG[CodiceConto] = "3300002" )<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>GruppoFiltrato = "MIX", Imp_Mix,<br>GruppoFiltrato = "PROPANO", Imp_Propano,<br>GruppoFiltrato = BLANK (), Imp_Mix + Imp_Propano<br>)` |
| Importo Movimento Mese | Delta valore magazzino nel mese (Acquisti - Vendite valorizzate CMP) | `// Calcola la variazione di valore del magazzino nel mese (Acquisti - Costo Venduto CMP)<br>[Importo Acquisto] - ([Qta Vendita] * [CMP Giacenza])` |
| Importo Giacenza | Valore giacenza calcolato come progressivo dei movimenti YTD | `// Calcola il valore della giacenza come: Iniziale + Somma dei delta mensili YTD<br>VAR GiacenzaCorrente =<br>[Importo Giacenza Iniziale] +<br>CALCULATE (<br>[Importo Movimento Mese],<br>DATESYTD ( DIM_Calendario[Date] )<br>)<br><br>RETURN<br>IF ( ABS ( GiacenzaCorrente ) > 100000000, BLANK (), GiacenzaCorrente )` |

## KPI_Vendite

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Importo Vendite (Doc) | Importo vendite basato su documenti di vendita | `// Restituisce l'importo totale delle vendite dai documenti<br>[Importo Vendita]` |
| Margine Cliente | Margine calcolato come differenza tra vendite e costo venduto | `// Calcola il margine assoluto per cliente<br>VAR Vendite = [Importo Vendite (Doc)]<br>VAR Costo = [Costo Venduto Cliente]<br>RETURN<br>Vendite - Costo` |
| Importo Acquisti | Importo totale acquisti | `// Importo totale degli acquisti<br>[Importo Acquisto]` |
| % Margine Cliente | Percentuale di margine sul cliente | `// Percentuale di marginalità sul fatturato<br>VAR Margine = [Margine Cliente]<br>VAR Vendite = [Importo Vendite (Doc)]<br>RETURN<br>DIVIDE ( Margine, Vendite, 0 )` |
| % Margine Cliente LY | Percentuale di margine sul cliente dello scorso anno | `// Marginalità percentuale dello stesso periodo dell'anno precedente<br>CALCULATE ( [% Margine Cliente], DATEADD ( DIM_Calendario[Date], -1, YEAR ) )` |
| % Crescita Margine | Percentuale di crescita del margine rispetto allo scorso anno | `// Variazione percentuale della marginalità rispetto all'anno precedente<br>VAR MargineAttuale = [% Margine Cliente]<br>VAR MarginePrecedente = [% Margine Cliente LY]<br>RETURN<br>DIVIDE (<br>MargineAttuale - MarginePrecedente,<br>MarginePrecedente,<br>0<br>)` |
| % Crescita YoY | Percentuale di crescita delle vendite anno su anno | `// Crescita percentuale del fatturato anno su anno<br>VAR VenditeAttuali = [Importo Vendite (Doc)]<br>VAR VenditePrecedenti = [Importo Vendite (Doc) LY]<br>RETURN<br>DIVIDE ( VenditeAttuali - VenditePrecedenti, VenditePrecedenti, 0 )` |
| Importo Vendite (Doc) LY | Importo vendite dello scorso anno | `// Fatturato dello stesso periodo dell'anno precedente<br>CALCULATE ( [Importo Vendite (Doc)], DATEADD ( DIM_Calendario[Date], -1, YEAR ) )` |
| Margine Cliente LY | Margine cliente dello scorso anno | `// Margine assoluto dello stesso periodo dell'anno precedente<br>CALCULATE ( [Margine Cliente], DATEADD ( DIM_Calendario[Date], -1, YEAR ) )` |
| Costo Venduto Cliente | Costo del venduto calcolato su quantità venduta e CMP | `// Valorizzazione del venduto: Quantità x Costo Medio Ponderato (CMP)<br>VAR Quantita = [Qta Vendita]<br>VAR CMP = [CMP Acquisto Prodotto]<br>RETURN<br>Quantita * CMP` |
| # Nuovi Clienti | Numero di nuovi clienti che non acquistavano da N giorni | `// Conta i clienti che hanno effettuato il primo acquisto nel periodo selezionato<br>// Un cliente è considerato "nuovo" se non ha acquistato nei precedenti N giorni (default 90)<br>VAR nr_giorni = SELECTEDVALUE(PARAM_NrGiorni[PARAM_NrGiorni], 90)<br>VAR Tab_Clienti_Attuali = VALUES ( 'FACT_DocumentoVendita'[CodiceClienteFatturazione] )<br><br>VAR Clienti_Storici =<br>CALCULATETABLE (<br>VALUES ( 'FACT_DocumentoVendita'[CodiceClienteFatturazione]  ),<br>FILTER (<br>ALL ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] > MIN ( DIM_Calendario[Date] ) - nr_giorni &&<br>DIM_Calendario[Date] < MIN ( DIM_Calendario[Date] )<br>)<br>)<br>RETURN<br>COUNTROWS (<br>EXCEPT ( Tab_Clienti_Attuali, Clienti_Storici )<br>)` |
| # Clienti Persi | Numero di clienti persi (non acquistano da 365 giorni) | `// Calcola i clienti "persi": quelli che hanno acquistato in passato (fino a 365 gg fa)<br>// ma non hanno effettuato acquisti negli ultimi N giorni (default 60)<br>VAR nr_giorni = 60<br>VAR Tab_Clienti_Attivi_Passato =<br>CALCULATETABLE (<br>VALUES ( 'FACT_DocumentoVendita'[CodiceClienteFatturazione]  ),<br>FILTER (<br>ALL ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] > MIN ( DIM_Calendario[Date] ) - 365 &&<br>DIM_Calendario[Date] <= MIN ( DIM_Calendario[Date] ) - nr_giorni<br>)<br>)<br>VAR Clienti_Recenti =<br>CALCULATETABLE (<br>VALUES ( 'FACT_DocumentoVendita'[CodiceClienteFatturazione]  ),<br>FILTER (<br>ALL ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] > MIN ( DIM_Calendario[Date] ) - nr_giorni &&<br>DIM_Calendario[Date] <= MIN ( DIM_Calendario[Date] )<br>)<br>)<br>RETURN<br>COUNTROWS ( EXCEPT ( Tab_Clienti_Attivi_Passato, Clienti_Recenti ) ) * -1` |
| Data Ultimo Acquisto Cliente | Data dell'ultima spedizione per il cliente | `// Restituisce la data dell'ultimo documento di vendita per il cliente nel contesto<br>LASTDATE('FACT_DocumentoVendita'[DataSpedizione])` |
| Data Ultimo Acquisto | Data massima di spedizione assoluta a sistema | `// Restituisce la data massima assoluta di vendita nel sistema (ignora i filtri temporali ma non quelli cliente)<br>MAXX(<br>ALL('FACT_DocumentoVendita'),<br>'FACT_DocumentoVendita'[DataSpedizione]<br>)` |
| # Giorni da ultimo Acquisto | Giorni trascorsi dall'ultimo acquisto del cliente rispetto alla data massima di sistema | `// Calcola la distanza in giorni tra l'ultima vendita assoluta e l'ultima vendita del cliente<br>VAR DataUltimaCliente = [Data Ultimo Acquisto Cliente]<br>VAR DataUltimaAssoluta = [Data Ultimo Acquisto]<br>RETURN<br>IF(<br>ISBLANK(DataUltimaCliente),<br>BLANK(),<br>VALUE(DataUltimaAssoluta - DataUltimaCliente)<br>)` |
| # Clienti Persi (2) | Clienti senza acquisti nel range temporale selezionato | `// Identifica i clienti che non hanno acquistato negli ultimi N giorni selezionati<br>VAR Lista_Tutti_Clienti = ALL('FACT_DocumentoVendita'[CodiceClienteFatturazione])<br>VAR nr_days = SELECTEDVALUE(PARAM_NrGiorni[PARAM_NrGiorni], 90)<br><br>VAR tab_periodo_controllo =<br>FILTER(<br>ALLSELECTED(DIM_Calendario),<br>DIM_Calendario[Date] > ( MIN(DIM_Calendario[Date]) - nr_days ) &&<br>DIM_Calendario[Date] < MIN(DIM_Calendario[Date])<br>)<br>VAR tab_clienti_senza_vendite =<br>FILTER(<br>Lista_Tutti_Clienti,<br>CALCULATE(<br>COUNTROWS('FACT_DocumentoVendita'),<br>tab_periodo_controllo<br>) = 0<br>)<br><br>RETURN<br>COUNTROWS(tab_clienti_senza_vendite)` |
| # Nuovi Clienti (2) | Clienti al loro primo acquisto assoluto nel periodo | `// Identifica i clienti che hanno acquistato ora ma non avevano MAI acquistato prima della data minima selezionata<br>VAR Clienti_Attuali = VALUES('FACT_DocumentoVendita'[CodiceClienteFatturazione])<br>VAR nr_days = SELECTEDVALUE(PARAM_NrGiorni[PARAM_NrGiorni], 90)<br><br>VAR tab_calendario_storico =<br>FILTER(<br>ALLSELECTED(DIM_Calendario),<br>DIM_Calendario[Date] < MIN(DIM_Calendario[Date])<br>)<br><br>VAR tab_nuovi =<br>FILTER(<br>Clienti_Attuali,<br>CALCULATE(<br>COUNTROWS('FACT_DocumentoVendita'),<br>tab_calendario_storico<br>) = 0<br>)<br><br>RETURN<br>CALCULATE(<br>COUNTROWS(VALUES('FACT_DocumentoVendita'[CodiceClienteFatturazione])),<br>tab_nuovi<br>)` |
| # Clienti Ritornati | Clienti che tornano ad acquistare dopo un periodo di inattività | `// Clienti che hanno acquistato nel periodo attuale e avevano acquistato in passato,<br>// ma non nel periodo immediatamente precedente (definito da nr_days)<br>VAR Clienti_Attuali = VALUES('FACT_DocumentoVendita'[CodiceClienteFatturazione])<br>VAR nr_days = SELECTEDVALUE(PARAM_NrGiorni[PARAM_NrGiorni], 90)<br><br>VAR tab_finestra_assenza =<br>FILTER(<br>ALLSELECTED(DIM_Calendario),<br>DIM_Calendario[Date] > ( MIN(DIM_Calendario[Date]) - nr_days ) &&<br>DIM_Calendario[Date] < MIN(DIM_Calendario[Date])<br>)<br><br>VAR tab_ritornati =<br>FILTER(<br>Clienti_Attuali,<br>CALCULATE(<br>COUNTROWS('FACT_DocumentoVendita'),<br>tab_finestra_assenza<br>) = 0<br>)<br><br>RETURN<br>COUNTROWS(tab_ritornati)` |
| Qta Vendite | Quantità totale venduta in tonnellate | `// Somma delle quantità in tonnellate filtrate per movimenti di tipo 'Vendita' in FACT_Magazzino<br>CALCULATE (<br>ABS ( [Qta Ton] ),<br>FACT_Magazzino[TipoMovimento] = "Vendita"<br>)` |
| Prezzo Medio Vendita | Prezzo medio unitario di vendita per categorie NAZ, UE, NAZPERM | `// Calcola il prezzo medio unitario per le categorie NAZ, UE e NAZPERM<br>CALCULATE(<br>AVERAGEX(<br>'FACT_DocumentoVendita',<br>'FACT_DocumentoVendita'[PrezzoUnitario]<br>),<br>'FACT_DocumentoVendita'[CodiceCategoria] IN {"NAZ", "UE", "NAZPERM"}<br>)` |
| Rank Cliente | Classifica del cliente per importo vendita | `// Classifica i clienti in base all'importo delle vendite<br>RANKX ( ALL ( DIM_Clienti ), [Importo Vendita], , DESC )` |
| Top 10 Clienti Vendite | Importo vendite per i primi 10 clienti | `// Restituisce l'importo vendite solo per i primi 10 clienti in classifica<br>VAR Classifica = [Rank Cliente]<br>RETURN<br>IF (<br>ISFILTERED ( DIM_Clienti[Nome] ),<br>IF ( Classifica <= 10, [Importo Vendite (CG)], BLANK () ),<br>CALCULATE (<br>[Importo Vendite (CG)],<br>TOPN ( 10, VALUES ( DIM_Clienti[Nome] ), [Importo Vendite (CG)] )<br>)<br>)` |
| Importo Vendite (CG) | Importo vendite basato sui movimenti di contabilità generale (CG) | `// Valore delle vendite derivante dalla Contabilità Generale (CG)<br>// Se presente l'importo clienti vendite usa quello, altrimenti usa i passaggi<br>VAR VenditeCG = [Importo Clienti Vendite]<br>VAR PassaggiCG = [Importo Clienti Passaggi]<br>RETURN<br>IF (<br>VenditeCG <> 0,<br>VenditeCG,<br>PassaggiCG<br>)` |
| Importo Clienti Passaggi | Importo vendite per passaggi (conti CG 31*) | `// Fatturato relativo ai conti di 'Passaggio' (Conti 31*)<br>CALCULATE (<br>ABS ( [Importo] ),<br>FACT_MovimentiCG[Conto] IN { "3100001", "3100002", "3100003", "3100004" }<br>)` |
| Importo Clienti Vendite | Importo ricavi da Contabilità Generale filtrato per conti 30* | `// Filtra i movimenti CG per i conti ricavi (30*) e per tipo Fattura/Nota Credito<br>VAR ContiRicavi = { "3000002", "3000003", "3000005" }<br>VAR ProdottoSelezionato = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto] )<br>VAR ProgettoCG =<br>SWITCH (<br>TRUE (),<br>ProdottoSelezionato = "PROPANO", "PROPAN",<br>ProdottoSelezionato = "MIX", "MIX",<br>BLANK ()<br>)<br><br>VAR TabellaFiltrata =<br>FILTER (<br>'FACT_MovimentiCG',<br>FACT_MovimentiCG[Conto] IN ContiRicavi &&<br>(ISBLANK(ProgettoCG) \|\| FACT_MovimentiCG[Progetto] = ProgettoCG) &&<br>FACT_MovimentiCG[Tipo Documento] IN { "Fattura", "Nota Credito" }<br>)<br><br>RETURN<br>CALCULATE ( ABS ( [Importo] ), TabellaFiltrata )` |
| Prezzo Dirette | Rapporto tra ricavi diretti CG e quantità venduta | `// Prezzo medio per le vendite dirette (Ricavi CG / Qta Ton)<br>DIVIDE ( [Importo Clienti Vendite], [Qta Vendite], 0 )` |
| Prezzo Passaggio | Rapporto tra ricavi passaggi CG e quantità venduta | `// Prezzo medio per i passaggi (Passaggi CG / Qta Ton)<br>DIVIDE ( [Importo Clienti Passaggi], [Qta Vendite], 0 )` |
| Importo Acquisti Progressivo (NAVI) | Costi totali navi accumulati nel tempo | `// Somma progressiva dei costi legati alle Navi (Acquisti)<br>CALCULATE (<br>[Total Costs NAVI],<br>FILTER (<br>ALL ( DIM_Calendario ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| # Clienti Vendita | Numero di clienti unici con movimenti di vendita | `// Conta i codici soggetti distinti che hanno effettuato una vendita in FACT_Magazzino<br>VAR Tab_Clienti_Vendita =<br>CALCULATETABLE (<br>VALUES ( FACT_Magazzino[Codice] ),<br>FACT_Magazzino[Tipo Movimento] = "Vendita"<br>)<br>RETURN<br>COUNTROWS ( Tab_Clienti_Vendita )` |
| Importo Vendite Gruppo Cliente | Fatturato segmentato per cluster di importo cliente | `// Calcola le vendite aggregate per gruppi definiti in base a soglie di importo<br>CALCULATE (<br>[Importo Vendite (CG)],<br>FILTER (<br>VALUES ( FACT_MovimentiCG[Importo] ),<br>COUNTROWS (<br>FILTER (<br>'Gruppo Vendite Clienti',<br>FACT_MovimentiCG[Importo] >= MIN ( 'Gruppo Vendite Clienti'[Min] )<br>&& FACT_MovimentiCG[Importo] < MAX ( 'Gruppo Vendite Clienti'[Max] )<br>)<br>) > 0<br>)<br>)` |
| Qta Vendite Progressivo | Somma progressiva delle quantità vendute (dal 2015) | `// Quantità venduta accumulata a partire dal 2015<br>VAR tab_periodo_storico =<br>FILTER (<br>ALL ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] >= DATE ( 2015, 1, 1 )<br>&& DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>RETURN<br>CALCULATE ( [Qta Vendite], tab_periodo_storico )` |
| Prezzo Medio Vendita Mix | Prezzo medio vendita per i prodotti Mix | `// Calcola il prezzo medio ponderato per i prodotti del gruppo MIX<br>VAR GruppoArticolo = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () )<br>VAR MeseCalendario = SELECTEDVALUE ( DIM_Calendario[MonthInCalendar] )<br><br>VAR ArticoliGruppo =<br>CALCULATETABLE (<br>SELECTCOLUMNS(DIM_Articoli, "@Codice", DIM_Articoli[CodiceArticolo]),<br>DIM_Articoli[GruppoProdotto] = GruppoArticolo<br>)<br><br>VAR TabellaMixFiltrata =<br>FILTER (<br>'FACT_Mix',<br>'FACT_Mix'[CodiceArticolo] IN ArticoliGruppo &&<br>'FACT_Mix'[MonthInCalendar] = MeseCalendario<br>)<br>RETURN<br>AVERAGEX (<br>TabellaMixFiltrata,<br>'FACT_Mix'[Prezzo Euro Mese]<br>)` |
| Prezzo Permute | Prezzo di riferimento per le permute | `// Il prezzo delle permute viene equiparato al Costo Medio Ponderato di acquisto<br>[CMP Acquisto]` |
| Importo Vendite | Fatturato teorico calcolato come Qta x Prezzo Medio | `// Calcolo teorico fatturato: Quantità Venduta x Prezzo Medio<br>[Qta Vendita] * [Prezzo Medio Vendita]` |
| Importo Vendite OLD2 | Misura legacy per calcolo vendite con switch logico | `// Logica di switch per determinare la fonte del fatturato in base al gruppo prodotto<br>VAR Gruppo = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () )<br>VAR Categoria = SELECTEDVALUE ( DIM_GruppiVendita[CodiceCategoria], BLANK () )<br>RETURN<br>SWITCH (<br>TRUE (),<br>Gruppo = "MIX", [Importo Clienti Vendite],<br>Gruppo = "PROPANO",<br>SWITCH (<br>TRUE (),<br>Categoria = "DIRETTE", [Importo Clienti Vendite],<br>Categoria = "PASSAGGIO", [Importo Clienti Passaggi],<br>BLANK ()<br>),<br>BLANK ()<br>)` |
| Prezzo Medio Vendita (CG) | Prezzo medio unitario derivato dai dati CG | `// Calcola il prezzo medio unitario basandosi sui ricavi di Contabilità Generale<br>DIVIDE ( [Importo Vendite (CG)], [Qta Vendite], 0 )` |
| Qta Vendite LY | Quantità venduta anno precedente | `// Quantità venduta nello stesso periodo dell'anno precedente<br>CALCULATE ( [Qta Vendite], DATEADD ( DIM_Calendario[Date], -1, YEAR ) )` |
| Prezzo Medio Vendita QTD | Prezzo medio vendita QTD | `// Media del prezzo di vendita accumulata nel trimestre (Quarter-To-Date)<br>TOTALQTD ( [Prezzo Medio Vendita], DIM_Calendario[Date] )` |
| Costo Venduto (Doc) | Costo del venduto basato su documenti e CMP | `// Valorizzazione del venduto basata su documenti: Qta Doc x CMP<br>VAR QtaDoc = [Qta Doc Vendita Articolo]<br>VAR CMP = [CMP Acquisto]<br>RETURN<br>QtaDoc * CMP` |
| # Clienti | Numero totale di clienti distinti | `// Conteggio univoco dei clienti che hanno documenti di vendita<br>DISTINCTCOUNT('FACT_DocumentoVendita'[CodiceClienteFatturazione])` |
| Importo Nuovi Clienti | Importo vendite generato dai nuovi clienti | `// Calcola il fatturato generato esclusivamente dai nuovi clienti (definizione N giorni)<br>VAR nr_giorni = SELECTEDVALUE(PARAM_NrGiorni[PARAM_NrGiorni], 90)<br>VAR Tab_Clienti_Attuali = VALUES ( 'FACT_DocumentoVendita'[CodiceClienteFatturazione] )<br><br>VAR Clienti_Storici =<br>CALCULATETABLE (<br>VALUES ( 'FACT_DocumentoVendita'[CodiceClienteFatturazione]  ),<br>FILTER (<br>ALL ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] > MIN ( DIM_Calendario[Date] ) - nr_giorni &&<br>DIM_Calendario[Date] < MIN ( DIM_Calendario[Date] )<br>)<br>)<br>VAR Tab_Nuovi = EXCEPT ( Tab_Clienti_Attuali, Clienti_Storici )<br><br>RETURN<br>CALCULATE(<br>[Importo Vendita],<br>Tab_Nuovi<br>)` |
| % Importo Nuovi Clienti | Percentuale di fatturato da nuovi clienti | `// Incidenza percentuale del fatturato dei nuovi clienti sul totale<br>DIVIDE([Importo Nuovi Clienti], [Importo Vendita], 0)` |
| % Nuovi Clienti | Percentuale di nuovi clienti sul totale | `// Incidenza percentuale del numero di nuovi clienti sul totale clienti<br>DIVIDE([# Nuovi Clienti], [# Clienti], 0)` |
| Importo Vendite (CMP) | Fatturato valorizzato al CMP di giacenza | `// Valorizzazione delle vendite al costo di giacenza (CMP)<br>[Qta Vendita] * [CMP Giacenza]` |

## KPI_VenditeDocumento

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Importo Doc Vendita | Importo lordo da documenti di vendita | `// Somma totale degli importi lordi presenti nei documenti di vendita<br>SUMX ( 'FACT_DocumentoVendita', 'FACT_DocumentoVendita'[Importo] )` |
| Importo Articolo Doc Vendita | Importo filtrato per righe di tipo Articolo | `// Importo filtrato esclusivamente per le righe di tipo 'Articolo' (esclude spese, conti, etc.)<br>CALCULATE (<br>[Importo Doc Vendita],<br>'FACT_DocumentoVendita'[TipoRiga] = "Articolo"<br>)` |
| Importo FT Doc Vendita | Importo totale fatture di vendita (FT VENDITA) | `// Importo delle sole fatture di vendita (FT VENDITA) per righe articolo<br>CALCULATE (<br>[Importo Doc Vendita],<br>'FACT_DocumentoVendita'[TipoRiga] = "Articolo" &&<br>'FACT_DocumentoVendita'[TipoDocumento] = "FT VENDITA"<br>)` |
| Importo NC Doc Vendita | Importo totale note di credito di vendita (NC VENDITA) | `// Importo totale delle note di credito di vendita (NC VENDITA)<br>CALCULATE (<br>[Importo Doc Vendita],<br>'FACT_DocumentoVendita'[TipoDocumento] = "NC VENDITA"<br>)` |
| Importo Netto Doc Vendita | Importo netto vendita (FT - Trasporto - NC) | `// Fatturato netto calcolato come: Fatture - Trasporti - Note di Credito<br>[Importo FT Doc Vendita] - [Importo Trasporto Doc Vendita] - [Importo NC Doc Vendita]` |
| Importo Costo Prodotto Doc Vendita | Importo del costo del prodotto basato sulla quantità e costo unitario | `// Valorizzazione del costo prodotto: Costo unitario x Quantità documenti<br>[Costo Prodotto] * [Qta Doc Vendita Articolo]` |
| Importo Margine Doc Vendita | Importo del margine calcolato su quantità e margine per tonnellata | `// Margine totale calcolato come: Margine per Ton x Quantità documenti<br>[Margine Ton Doc Vendita] * [Qta Doc Vendita Articolo]` |
| Importo FT PASSAGGIO Doc Vendita | Importo fatture per movimenti di passaggio | `// Importo delle fatture di passaggio (TipoRiga = Conto) ignorando i filtri sugli articoli<br>CALCULATE (<br>[Importo Doc Vendita],<br>REMOVEFILTERS ( DIM_Articoli ),<br>'FACT_DocumentoVendita'[TipoRiga] = "Conto",<br>'FACT_DocumentoVendita'[TipoDocumento] = "FT VENDITA"<br>)` |
| Importo NC PASSAGGIO Doc Vendita | Importo note di credito per movimenti di passaggio | `// Importo delle note di credito per i movimenti di passaggio<br>CALCULATE (<br>[Importo Doc Vendita],<br>'FACT_DocumentoVendita'[TipoRiga] = "Conto" &&<br>'FACT_DocumentoVendita'[TipoDocumento] = "NC VENDITA"<br>)` |
| Importo Netto PASSAGGIO Doc Vendita | Importo netto per movimenti di passaggio | `// Fatturato netto dei passaggi: Fatture - Note di Credito<br>[Importo FT PASSAGGIO Doc Vendita] - [Importo NC PASSAGGIO Doc Vendita]` |
| Importo Netto FILTER Doc Vendita | Importo netto filtrato dinamicamente tra Passaggi e Vendite Dirette | `// Switch dinamico dell'importo netto in base al gruppo vendita selezionato (PASSAGGIO vs DIRETTI)<br>IF (<br>SELECTEDVALUE ( DIM_GruppiVendita[Gruppo] ) = "PASSAGGIO",<br>[Importo Netto PASSAGGIO Doc Vendita],<br>[Importo Netto Doc Vendita]<br>)` |
| Importo Controllo (Doc - CG) | Differenza tra importo da documenti e importo da contabilità generale | `// Delta di controllo tra il fatturato calcolato dai documenti e quello da Contabilità Generale<br>KPI_Vendite[Importo Vendite] - KPI_Vendite[Importo Vendite (CG)]` |
| Importo Trasporto Doc Vendita | Importo totale dei costi di trasporto da documenti | `// Somma dei costi di trasporto esplicitati nelle righe dei documenti di vendita<br>SUMX ( 'FACT_DocumentoVendita', 'FACT_DocumentoVendita'[CostoTrasporto] )` |
| Importo Doc Vendita - Trasporto | Importo lordo documenti al netto del trasporto | `// Importo lordo dei documenti depurato dai costi di trasporto<br>[Importo Doc Vendita] - [Importo Trasporto Doc Vendita]` |
| Importo Vendita | Importo vendita principale (netto documenti) | `// Alias per l'importo netto da documenti di vendita<br>[Importo Netto Doc Vendita]` |
| Importo Acquisto Cliente | Importo stimato di acquisto per il cliente basato sul CMP | `// Stima del costo di acquisto per il volume venduto al cliente (Qta x CMP)<br>[Qta Vendita] * [CMP Acquisto]` |
| Importo Vendita % | Percentuale dell'importo vendita del cliente sul totale | `// Incidenza percentuale delle vendite del cliente corrente sul totale vendite del periodo<br>VAR VenditeCliente = [Importo Vendita]<br>VAR TotaleVendite =<br>CALCULATE (<br>[Importo Vendita],<br>REMOVEFILTERS ( DIM_Clienti[Nome] )<br>)<br>RETURN<br>DIVIDE ( VenditeCliente, TotaleVendite, 0 )` |
| Importo Vendita N DAYS | Importo vendita riferito agli ultimi N giorni (default 90) | `// Calcola l'importo delle vendite riferito a una finestra temporale di N giorni (default 90)<br>VAR NrOfDays = 90<br>VAR Risultato =<br>CALCULATE(<br>[Importo Vendita],<br>DATEADD(DIM_Calendario[Date], -NrOfDays, DAY)<br>)<br>RETURN<br>Risultato` |
| Importo Vendita IPEM N DAYS | Importo vendita deposito IPEM negli ultimi N giorni | `// Calcola le vendite del deposito IPEM riferite a una finestra di N giorni<br>VAR NrOfDays = 90<br>VAR ImportoVenditaIPEM =<br>CALCULATE(<br>[Importo Vendita],<br>'FACT_DocumentoVendita'[CodiceDeposito] = "IPEM"<br>)<br><br>VAR Risultato =<br>CALCULATE(<br>ImportoVenditaIPEM,<br>DATEADD(DIM_Calendario[Date], -NrOfDays, DAY)<br>)<br>RETURN<br>Risultato` |
| Qta Doc Vendita Articolo | Quantità venduta per righe di tipo Articolo | `// Quantità totale per le sole righe di tipo 'Articolo'<br>CALCULATE (<br>[Qta Doc Vendita],<br>'FACT_DocumentoVendita'[TipoRiga] = "Articolo"<br>)` |
| Qta Doc Vendita Cliente | Quantità venduta filtrata per il cliente selezionato | `// Quantità venduta specifica per il cliente nel contesto corrente<br>CALCULATE (<br>[Qta Doc Vendita Articolo],<br>DIM_Clienti[CodiceCliente] = SELECTEDVALUE ( DIM_Clienti[CodiceCliente] )<br>)` |
| Qta Doc Vendita | Quantità totale da documenti di vendita | `// Somma totale delle quantità presenti nei documenti di vendita<br>SUMX ( 'FACT_DocumentoVendita', 'FACT_DocumentoVendita'[Quantity] )` |
| Qta Doc Vendita IPEM | Quantità venduta da deposito IPEM | `// Quantità venduta dal deposito IPEM per le righe articolo<br>CALCULATE (<br>[Qta Doc Vendita Articolo],<br>'FACT_DocumentoVendita'[CodiceDeposito] = "IPEM"<br>)` |
| Qta Doc Vendita FT | Quantità totale fatture di vendita | `// Quantità totale caricata nelle fatture di vendita<br>CALCULATE (<br>[Qta Doc Vendita Articolo],<br>'FACT_DocumentoVendita'[TipoDocumento] = "FT VENDITA"<br>)` |
| Qta Doc Vendita NC | Quantità totale note di credito di vendita | `// Quantità totale stornata nelle note di credito di vendita<br>CALCULATE (<br>[Qta Doc Vendita],<br>'FACT_DocumentoVendita'[TipoDocumento] = "NC VENDITA"<br>)` |
| Qta Doc Vendita Netta | Quantità netta venduta (FT - NC) | `// Differenza tra quantità fatturata e quantità stornata (FT - NC)<br>[Qta Doc Vendita FT] - [Qta Doc Vendita NC]` |
| Qta Doc Vendita Gruppo Articolo | Quantità venduta per gruppo registrazione prodotto | `// Quantità venduta filtrata per gruppo registrazione prodotto (MIX/PROPANO)<br>VAR Gruppo = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto] )<br>RETURN<br>IF(Gruppo <> BLANK(),<br>CALCULATE (<br>[Qta Doc Vendita],<br>'FACT_DocumentoVendita'[GruppoRegistrazioneProdotto] = Gruppo<br>),<br>0<br>)` |
| Qta Vendita | Misura principale per la quantità venduta | `// Misura principale per la quantità venduta (netta FT - NC)<br>[Qta Doc Vendita Netta]` |
| Qta Vendita ALL | Quantità totale venduta (senza filtri) | `// Quantità totale venduta ignorando tutti i filtri contestuali sulla tabella documenti<br>SUMX ( ALL ( 'FACT_DocumentoVendita' ), [Qta Vendita] )` |
| Qta Doc Vendita 3000002 | Quantità venduta per l'articolo specifico 3000002 | `// Quantità venduta specificamente per l'articolo 3000002 in fattura<br>CALCULATE (<br>[Qta Doc Vendita],<br>'FACT_DocumentoVendita'[CodiceArticolo] = "3000002" &&<br>'FACT_DocumentoVendita'[TipoDocumento] = "FT VENDITA"<br>)` |
| Qta Vendita AVG YTD Monthly | Media mensile della quantità venduta YTD | `// Media della quantità venduta giornaliera calcolata sul periodo YTD<br>CALCULATE(<br>[Qta Vendita AVG Day],<br>ALL(DIM_Calendario),<br>DIM_Calendario[MonthInCalendar] <= MAX(DIM_Calendario[MonthInCalendar]) &&<br>DIM_Calendario[Year] = MAX(DIM_Calendario[Year])<br>)` |
| Qta Vendita IPEM | Quantità venduta da deposito IPEM (misura base) | `// Quantità venduta netta dal deposito IPEM<br>CALCULATE(<br>[Qta Vendita],<br>'FACT_DocumentoVendita'[CodiceDeposito] = "IPEM"<br>)` |
| Qta Vendita Media Pesata | Quantità media pesata sulle vendite mensili | `// Calcola la quantità media pesando i mesi in base al loro volume rispetto all'anno<br>VAR QtaAnno =<br>CALCULATE(<br>[Qta Vendita],<br>ALL(DIM_Calendario[MonthName], DIM_Calendario[MonthOfYear])<br>)<br>VAR TabellaMesi =<br>ADDCOLUMNS(<br>SUMMARIZE(<br>'FACT_DocumentoVendita',<br>DIM_Calendario[Year],<br>DIM_Calendario[MonthName]<br>),<br>"Qta_Mese", [Qta Vendita],<br>"Peso", DIVIDE([Qta Vendita], QtaAnno, 0)<br>)<br>RETURN<br>SUMX(<br>TabellaMesi,<br>[Peso] * [Qta_Mese]<br>)` |
| Qta Vendita Cliente CUM | Quantità cumulata per classifica clienti | `// Calcola il totale cumulato della quantità venduta seguendo il ranking dei clienti<br>VAR ClienteRank =<br>RANKX(<br>ALL(DIM_Clienti[Nome & Codice]),<br>[Qta Vendita],,<br>DESC,<br>Dense<br>)<br>VAR TotaleCumulato =<br>CALCULATE(<br>[Qta Vendita],<br>FILTER(<br>ALL(DIM_Clienti[Nome & Codice]),<br>ClienteRank >= RANKX(<br>ALL(DIM_Clienti[Nome & Codice]),<br>[Qta Vendita],,<br>DESC,<br>Dense<br>)<br>)<br>)<br>RETURN<br>IF( [Qta Vendita] <> BLANK(), TotaleCumulato )` |
| Importo Netto Doc Vendita LM CUM | Importo netto mese precedente cumulato | `// Importo netto del mese precedente (LM) accumulato nel tempo<br>CALCULATE (<br>[Importo Netto Doc Vendita LM],<br>FILTER (<br>ALLSELECTED ( DIM_Calendario ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| Importo Netto Doc Vendita -2M CUM | Importo netto 2 mesi fa cumulato | `// Importo netto di due mesi fa accumulato nel tempo<br>CALCULATE (<br>[Importo Netto Doc Vendita -2M],<br>FILTER (<br>ALLSELECTED ( DIM_Calendario ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| Importo Vendite CY-LY | Differenza importo vendite tra anno corrente e precedente | `// Variazione assoluta del fatturato netto tra l'anno corrente e l'anno precedente<br>[Importo Netto FILTER Doc Vendita] - CALCULATE([Importo Netto FILTER Doc Vendita], SAMEPERIODLASTYEAR(DIM_Calendario[Date]))` |
| Qta Vendita Forecast CUM | Quantità forecast cumulata | `// Somma progressiva della quantità di forecast<br>SUMX(<br>FILTER (<br>ALLSELECTED ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>),<br>[Qta Vendita Forecast]<br>)` |
| Qta Vendita Forecast MA 30D | Media mobile 30 giorni per il forecast quantità | `// Media mobile a 30 giorni per il forecast, calcolata solo sui giorni lavorativi (esclude weekend)<br>VAR GiornoSettimana = SELECTEDVALUE ( DIM_Calendario[DayOfWeekName] )<br>VAR IsWeekend = GiornoSettimana IN { "sabato", "domenica" }<br>RETURN<br>IF( ISBLANK([Qta Vendita]) && NOT(IsWeekend),<br>AVERAGEX (<br>DATESINPERIOD ( DIM_Calendario[Date], LASTDATE ( DIM_Calendario[Date] ), -30, DAY ),<br>[Qta Vendita]<br>),<br>BLANK ()<br>)` |
| Qta Vendita Forecast TOT | Forecast totale quantità combinato | `// Forecast totale che combina la media mobile 30gg applicata allo stesso periodo dell'anno precedente<br>IF (<br>ISBLANK ( [Qta Vendita] ),<br>CALCULATE (<br>[Qta Vendita Forecast MA 30D],<br>SAMEPERIODLASTYEAR ( DIM_Calendario[Date] )<br>),<br>BLANK ()<br>)` |
| Qta Vendita Forecast Remain | Forecast rimanente dalla data dell'ultima vendita | `// Calcola il forecast per il periodo residuo, partendo dall'ultima data di vendita registrata<br>VAR DataUltimaVendita = CALCULATE ( MAX ( 'FACT_DocumentoVendita'[DataSpedizione] ), REMOVEFILTERS () )<br>VAR ForecastResiduo =<br>CALCULATE (<br>[Qta Vendita Forecast],<br>KEEPFILTERS ( DIM_Calendario[Date] >= DataUltimaVendita )<br>)<br>RETURN<br>ForecastResiduo` |
| Qta Vendita & Forecast | Combinazione di quantità venduta reale e forecast rimanente | `// Totale quantità reale (consuntivo) + quantità forecast per il periodo residuo<br>[Qta Vendita] + [Qta Vendita Forecast Remain]` |
| Qta Vendita CUM vs Forecast CUM | Differenza tra quantità cumulata reale e cumulata forecast | `// Delta tra il totale cumulato reale e il totale cumulato previsto (forecast)<br>IF (<br>ISBLANK ( [Qta Vendita CUM] ),<br>BLANK (),<br>[Qta Vendita CUM] - [Qta Vendita Forecast CUM]<br>)` |
| Qta Vendita Forecast Remain CUM | Forecast rimanente cumulato | `// Somma progressiva del forecast rimanente<br>CALCULATE (<br>[Qta Vendita Forecast Remain],<br>FILTER (<br>ALLSELECTED ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| Qta Vendita Forecast Remain Mediana | Mediana del forecast rimanente | `// Mediana del forecast rimanente nel contesto calendario<br>MEDIANX ( DIM_Calendario, [Qta Vendita Forecast Remain] )` |
| Regression Line | Retta di regressione lineare sulle vendite cumulate | `// Calcola i punti della retta di regressione lineare (Y = a + bX) per la quantità cumulata<br>VAR TabellaDati =<br>FILTER (<br>SELECTCOLUMNS (<br>ALLSELECTED ( DIM_Calendario ),<br>"x_val", DIM_Calendario[Date],<br>"y_val", [Qta Vendita CUM]<br>),<br>NOT ( ISBLANK ( [x_val] ) ) && NOT ( ISBLANK ( [y_val] ) )<br>)<br>VAR SumY = SUMX ( TabellaDati, [y_val] )<br>VAR SumX = SUMX ( TabellaDati, [x_val] )<br>VAR SumX2 = SUMX ( TabellaDati, [x_val] ^ 2 )<br>VAR SumXY = SUMX ( TabellaDati, [x_val] * [y_val] )<br>VAR N = COUNTROWS ( TabellaDati )<br><br>VAR Intercetta = DIVIDE ( ( SumY * SumX2 - SumX * SumXY ), ( N * SumX2 - ( SumX ) ^ 2 ), 0 )<br>VAR Pendenza = DIVIDE ( ( N * SumXY - SumX * SumY ), ( N * SumX2 - ( SumX ) ^ 2 ), 0 )<br><br>RETURN<br>SUMX ( DIM_Calendario, Pendenza * DIM_Calendario[Date] + Intercetta )` |
| Regression Slope | Pendenza della retta di regressione (velocità di vendita) | `// Calcola solo la pendenza (coefficiente angolare) della retta di regressione lineare<br>VAR TabellaDati =<br>FILTER (<br>SELECTCOLUMNS (<br>ALLSELECTED ( DIM_Calendario ),<br>"x_val", DIM_Calendario[Date],<br>"y_val", [Qta Vendita CUM]<br>),<br>NOT ( ISBLANK ( [x_val] ) ) && NOT ( ISBLANK ( [y_val] ) )<br>)<br>VAR SumY = SUMX ( TabellaDati, [y_val] )<br>VAR SumX = SUMX ( TabellaDati, [x_val] )<br>VAR SumX2 = SUMX ( TabellaDati, [x_val] ^ 2 )<br>VAR SumXY = SUMX ( TabellaDati, [x_val] * [y_val] )<br>VAR N = COUNTROWS ( TabellaDati )<br><br>VAR Pendenza = DIVIDE ( ( N * SumXY - SumX * SumY ), ( N * SumX2 - ( SumX ) ^ 2 ), 0 )<br>RETURN<br>Pendenza` |
| Qta Vendita ToDo | Quantità ancora da vendere per raggiungere l'obiettivo | `// Calcola la quantità mancante per raggiungere l'obiettivo di vendita fissato<br>VAR Differenza = [Valore Obiettivo Vendite] - [Qta Vendita ALL]<br>RETURN<br>IF(Differenza >= 0, Differenza, 0)` |
| Giorni Vendita x Obiettivo | Stima dei giorni necessari per raggiungere l'obiettivo | `// Stima il numero di giorni necessari a raggiungere l'obiettivo dividendo il ToDo per la pendenza di regressione<br>VAR GiorniStimati = DIVIDE ( [Qta Vendita ToDo], [Regression Slope], 0 )<br>RETURN<br>IF( GiorniStimati >= 0, GiorniStimati, 0 )` |
| Data Obiettivo Vendita | Data prevista per il raggiungimento dell'obiettivo di vendita | `// Proiezione della data di raggiungimento obiettivo aggiungendo i giorni stimati ad oggi<br>TODAY() + [Giorni Vendita x Obiettivo]` |
| Importo Vendita AVG N Days LQ | Media importo vendita N giorni, riferita al trimestre precedente | `// Media dell'importo vendita negli ultimi N giorni, riferita al trimestre precedente (Last Quarter)<br>CALCULATE (<br>[Importo Vendita AVG N Days],<br>DATEADD ( 'FACT_DocumentoVendita'[DataRegistrazione], -1, QUARTER )<br>)` |
| Qta Vendita Forecast | Forecast quantità basato su medie storiche annuali | `// Forecast quantità basato sulla media storica mensile degli anni passati (fino ad oggi)<br>VAR PrimoAnno = VALUE(MIN(DIM_Calendario[YEAR]))<br>VAR TabellaStorica =<br>FILTER(<br>SUMMARIZE(<br>DIM_Calendario,<br>DIM_Calendario[Year],<br>DIM_Calendario[MonthName],<br>"Qta_Vendite", [Qta Vendita]<br>),<br>VALUE(DIM_Calendario[Year]) >= PrimoAnno && VALUE(DIM_Calendario[Year]) <= YEAR(TODAY())<br>)<br><br>RETURN<br>AVERAGEX(TabellaStorica, [Qta_Vendite])` |
| Qta Vendita Forecast Mediana | Forecast quantità basato su mediana storica | `// Forecast quantità basato sulla mediana storica mensile degli anni passati (escludendo i mesi a zero)<br>VAR PrimoAnno = VALUE(MIN(DIM_Calendario[YEAR]))<br>VAR TabellaStorica =<br>FILTER(<br>SUMMARIZE(<br>DIM_Calendario,<br>DIM_Calendario[Year],<br>DIM_Calendario[MonthName],<br>"Qta_Vendite", [Qta Vendita]<br>),<br>VALUE(DIM_Calendario[Year]) >= PrimoAnno &&<br>VALUE(DIM_Calendario[Year]) <= YEAR(TODAY()) &&<br>[Qta Vendita] > 0<br>)<br><br>RETURN<br>MEDIANX(TabellaStorica, [Qta_Vendite])` |
| Prezzo Netto Doc Vendita OLD | Prezzo netto da documenti (versione precedente) | `// Calcolo legacy del prezzo netto unitario: Importo Netto / Quantità Articolo<br>DIVIDE ( [Importo Netto FILTER Doc Vendita], [Qta Doc Vendita Articolo], 0 )` |
| Costo Prodotto | Calcolo del costo prodotto per cliente e anno | `// Calcola il costo prodotto aggregato per cliente e anno confrontando vendite documenti e costi CG<br>VAR CodiceCliente = SELECTEDVALUE ( DIM_Clienti[CodiceCliente] )<br>VAR AnnoFiltro = SELECTEDVALUE ( DIM_Calendario[Year] )<br>VAR QtaCliente =<br>CALCULATE (<br>[Qta Doc Vendita Articolo],<br>'FACT_DocumentoVendita'[CodiceClienteFatturazione] = CodiceCliente &&<br>YEAR ( 'FACT_DocumentoVendita'[DataSpedizione] ) = VALUE ( AnnoFiltro )<br>)<br>VAR CostoTotaleGPL =<br>CALCULATE (<br>[COSTO GPL],<br>YEAR ( FACT_MovimentiCG[DataRegistrazione] ) = VALUE ( AnnoFiltro )<br>)<br>RETURN<br>QtaCliente & "_" & CostoTotaleGPL` |
| Costo Prodotto 2 | Costo prodotto specifico per cliente C00033 | `// Costo prodotto filtrato specificamente per il cliente C00033<br>CALCULATE ( [Costo Prodotto], DIM_Clienti[CodiceCliente] = "C00033" )` |
| % Importo Margine Doc Vendita | Percentuale del margine sull'importo netto da documenti | `// Incidenza percentuale del margine sull'importo netto dei documenti<br>DIVIDE ( [Importo Margine Doc Vendita], [Importo Netto Doc Vendita], 0 )` |
| Margine Ton Doc Vendita | Margine per tonnellata da documenti (Prezzo - CMP) | `// Margine unitario per tonnellata: Prezzo Netto - CMP Acquisto<br>[Prezzo Netto Doc Vendita OLD] - [CMP Acquisto]` |
| Prezzo Netto Doc Vendita | Prezzo netto unitario da documenti | `// Prezzo netto unitario: Importo Netto / Quantità Fatturata<br>DIVIDE ( [Importo Netto FILTER Doc Vendita], [Qta Doc Vendita FT], 0 )` |
| Prezzo Trasporto Doc Vendita | Prezzo medio di trasporto da documenti | `// Media dei costi di trasporto unitari registrati nelle righe articolo dei documenti<br>CALCULATE (<br>AVERAGEX ( 'FACT_DocumentoVendita', 'FACT_DocumentoVendita'[CostoTrasporto] ),<br>'FACT_DocumentoVendita'[TipoRiga] = "Articolo"<br>)` |
| Prezzo Doc Vendita - Trasporto | Prezzo al netto del trasporto | `// Prezzo unitario al netto del trasporto: (Importo - Trasporto) / Qta Gruppo Articolo<br>DIVIDE (<br>[Importo Doc Vendita - Trasporto],<br>[Qta Doc Vendita Gruppo Articolo],<br>0<br>)` |
| Prezzo Vendita | Prezzo vendita medio per categorie principali | `// Calcola il prezzo medio di vendita filtrando per le categorie principali di ricavo<br>CALCULATE(<br>DIVIDE([Importo Vendita], [Qta Vendita], 0),<br>'FACT_DocumentoVendita'[CodiceCategoria] IN {"NAZ", "UE", "NAZPERM"}<br>)` |
| Qta Vendita Forecast Media 2 | Media del forecast quantità sull'intero calendario | `// Media complessiva del forecast quantità<br>AVERAGEX(DIM_Calendario, [Qta Vendita Forecast])` |
| Qta Vendita Forecast NEW | Forecast quantità basato su CAGR (Compound Annual Growth Rate) | `// Forecast quantità basato sulla proiezione CAGR (Compound Annual Growth Rate) storica<br>VAR AnnoCorrente = SELECTEDVALUE(DIM_Calendario[Year])<br>VAR PrimoAnno = MIN(DIM_Calendario[Year])<br>VAR UltimoAnno = MAX(DIM_Calendario[Year])<br>VAR NrAnniProiezione = AnnoCorrente - UltimoAnno<br><br>VAR QtaUltimoAnno = CALCULATE([Qta Vendita], DIM_Calendario[Year] = UltimoAnno)<br>VAR QtaPrimoAnno = CALCULATE([Qta Vendita], DIM_Calendario[Year] = PrimoAnno)<br><br>VAR CAGR = POWER(<br>DIVIDE(QtaUltimoAnno, QtaPrimoAnno),<br>DIVIDE(1, UltimoAnno - PrimoAnno)<br>) - 1<br><br>VAR ForecastCAGR = QtaUltimoAnno * POWER((1 + CAGR), NrAnniProiezione)<br><br>RETURN<br>IF( AnnoCorrente > UltimoAnno, ForecastCAGR )` |
| Qta Vendita Data Reg | Quantità venduta riferita alla Data Registrazione (relazione inattiva) | `// Forza il calcolo delle quantità utilizzando la relazione inattiva con DataRegistrazione<br>CALCULATE(<br>[Qta Vendita],<br>USERELATIONSHIP(DIM_Calendario[Date], 'FACT_DocumentoVendita'[DataRegistrazione])<br>)` |
| Qta Vendita Forecast 3Y | Forecast quantità basato sulla media degli ultimi 3 anni | `// Forecast semplificato basato sulla media delle quantità degli ultimi 3 anni (LY, 2LY, 3LY)<br>DIVIDE([Qta Vendita LY] + [Qta Vendita 2LY] + [Qta Vendita 3LY], 3, 0)` |
| Qta Vendita Forecast 3Y CUM | Forecast 3 anni cumulato | `// Somma progressiva del forecast 3 anni<br>CALCULATE (<br>[Qta Vendita Forecast 3Y],<br>FILTER (<br>ALLSELECTED ( DIM_Calendario[Date] ),<br>DIM_Calendario[Date] <= MAX ( DIM_Calendario[Date] )<br>)<br>)` |
| Qta Vendita NEW | Nuova misura quantità per test star schema | `// Nuova misura di quantità per test architettura Star Schema (mappa articoli su MIX e PROPANO)<br>VAR GruppoProdotto = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () )<br><br>VAR QtaMix = CALCULATE(<br>SUMX('FACT_Vendite', 'FACT_Vendite'[Quantity]),<br>'FACT_Vendite'[CodiceArticolo] IN {"1", "1T"}<br>)<br><br>VAR QtaPropano = CALCULATE(<br>SUMX('FACT_Vendite', 'FACT_Vendite'[Quantity]),<br>'FACT_Vendite'[CodiceArticolo] IN {"2", "2T"}<br>)<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>GruppoProdotto = "MIX", QtaMix,<br>GruppoProdotto = "PROPANO", QtaPropano,<br>QtaMix + QtaPropano<br>)` |
| Bar | Base per grafico Target vs Actual | `// Restituisce il valore minimo tra il reale e il forecast (utilizzata per visualizzazione grafici target)<br>MIN([Importo Vendita], [Importo Vendita Forecast])` |
| Importo Vendita Forecast | Forecast importo vendita basato su medie storiche | `// Forecast del fatturato basato sulla media storica mensile degli anni passati<br>VAR PrimoAnno = VALUE(MIN(DIM_Calendario[YEAR]))<br>VAR TabellaStorica =<br>FILTER(<br>SUMMARIZE(<br>DIM_Calendario,<br>DIM_Calendario[Year],<br>DIM_Calendario[MonthName],<br>"Importo_Vendite", [Importo Vendita]<br>),<br>VALUE(DIM_Calendario[Year]) >= PrimoAnno && VALUE(DIM_Calendario[Year]) <= YEAR(TODAY())<br>)<br><br>VAR MediaStorica = AVERAGEX(TabellaStorica, [Importo_Vendite])<br>RETURN<br>IF( MediaStorica <> BLANK(), MediaStorica, 0 )` |
| Unmet Bar | Quota mancante per raggiungere il target (grafico) | `// Calcola la quota di target non ancora raggiunta per la visualizzazione a barre<br>VAR Sales = [Importo Vendita]<br>VAR Target = [Importo Vendita Forecast]<br>VAR Marker = [Marker Value]<br>VAR Base = [Bar]<br>RETURN<br>IF( Target > Sales, Marker - Base )` |
| Met Bar | Quota raggiunta del target (grafico) | `// Calcola la quota di target superata per la visualizzazione a barre<br>VAR Sales = [Importo Vendita]<br>VAR Target = [Importo Vendita Forecast]<br>VAR Marker = [Marker Value]<br>VAR Base = [Bar]<br>RETURN<br>IF( Sales >= Target, Marker - Base )` |
| Marker Value | Valore massimo tra vendita e target (per marker grafico) | `// Valore massimo tra il consuntivo e il target per definire il limite del marker nel grafico<br>MAX([Importo Vendita], [Importo Vendita Forecast])` |
| Qta Vendita PREV | Quantità prevista tramite LINEST | `// Proiezione della quantità venduta utilizzando i coefficienti Slope e Intercept calcolati via LINEST<br>SUM(LinestResult[Intercept]) + SUM(LinestResult[Slope1]) * SUM('FACT_Vendite'[Date])` |
| Qta Vendita CONTINUA | Quantità venduta continua (riempie i vuoti con la media) | `// Quantità venduta che riempie i valori nulli (gap) con la media mensile (utile per analisi di trend)<br>IF(ISBLANK([Qta Vendita]), [Qta Vendita AVG Month], [Qta Vendita])` |
| Qta Vendita AVG Month | Media mensile della quantità venduta | `// Media semplice delle quantità vendute aggregate per mese e anno<br>AVERAGEX ( VALUES(DIM_Calendario[MonthnYear]), [Qta Vendita] )` |
| Diff Actual/Previous | Differenza tra valore attuale e periodo precedente | `// Calcola la variazione tra il valore della misura selezionata e lo stesso valore nel periodo precedente<br>[Value Previous] - SELECTEDMEASURE()` |
| Qta Vendita Prodotto | Quantità venduta per prodotto con logica di switch | `// Calcola la quantità venduta totale applicando uno switch logico tra i prodotti (PROPANO/MIX)<br>VAR GruppoProdotto = SELECTEDVALUE ( DIM_Articoli[GruppoProdotto], BLANK () )<br><br>VAR QtaPropano = [Qta Vendita]<br>VAR QtaMix = [Qta Doc Vendita IPEM]<br>VAR QtaTotale = QtaPropano + QtaMix<br><br>RETURN<br>SWITCH (<br>TRUE (),<br>GruppoProdotto = "PROPANO", QtaPropano,<br>GruppoProdotto = "MIX", QtaMix,<br>QtaTotale<br>)` |

## PARAM_NrGiorni

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| Valore PARAM_NrGiorni |  | `SELECTEDVALUE('PARAM_NrGiorni'[PARAM_NrGiorni], 90)` |

## PARAM_TimeRange

| Measure Name | Description | Formula |
|--------------|-------------|---------|
| PARAM_ValoreTimeRange |  | `SELECTEDVALUE('PARAM_TimeRange'[PARAM_TimeRange], 3)` |
